# 演示模式：`npm run demo`（vite --mode demo）读取本文件。
# 打开后前端完全离线运行：登录/项目/视频库/标注/审核/片段库/导出/项目管理
# 均由 src/demo/** 的本地模拟数据驱动，不请求后端，也不执行真实 ffmpeg。
# 普通 `npm run dev` / `npm run build` 不读取本文件，仍走真实 API（VITE_API_BASE）。
VITE_DEMO_MODE=true
