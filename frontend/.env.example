# 复制为 .env 使用（.env 已被 gitignore）
# 后端 API 根地址（不含 /api 后缀由客户端自动拼接）
VITE_API_BASE=http://localhost:8000/api

# 演示模式：不要写在这里，请使用仓库内的 .env.demo
# 启动方式：npm run demo（读取 .env.demo，离线运行，无需后端）
# 关闭方式：普通 npm run dev / npm run build（不读取 .env.demo，走真实 API）
VITE_DEMO_MODE=false
