# 数据标注网站需求文档

> 版本：v0.1  
> 日期：2026-08-05  
> 对应工作项：WI-20260805-22

## 1. 项目背景

本项目处于小鼠行为识别阶段。上游已完成 YOLO 姿态估计与跨帧追踪，获得每只小鼠的长期身份、位置和关键点信息。本阶段的最终目标是从多小鼠顶视视频中提取可回溯的社会行为事件。

标注网站是行为识别管线中的人工标注环节，替代原先的 BORIS 桌面单机方案，提供**多人在线协作标注与审核**能力。

## 2. 标注场景

### 2.1 输入

- **原始视频**：顶视角群养小鼠长视频。
- 视频可能预先烧录 YOLO 检测框和身份 ID（待定，详见第 7 节）。

### 2.2 标注操作

1. 标注者在视频时间轴上标记行为片段的**开始时间和结束时间**。
2. 为片段指定**行为类别**（多分类，具体类别由行为字典定义）。
3. 可选：在视频画面上手动拖框，裁切出发生行为的**局部区域**。
4. 不标记小鼠个体身份。

### 2.3 输出

- 按标注区间**导出视频片段文件**（服务端 ffmpeg 裁剪）。
- **统一事件格式 JSON**，兼容现有行为识别下游：
  ```json
  {
    "video_id": "video_01",
    "start_time": 12.4,
    "end_time": 14.84,
    "start_frame": 310,
    "end_frame": 371,
    "behavior": "attack",
    "crop_region": null,
    "confidence": "certain",
    "annotator": "annotator_01",
    "reviewer": null,
    "review_status": "pending"
  }
  ```

> 各字段含义和约束详见 `行为识别/调研/行为标注工具调研.md` §5。

## 3. 核心功能

### 3.1 视频管理

- 上传原始视频，记录视频元信息（文件名、时长、帧率、分辨率）。
- 列表浏览已上传视频，支持搜索和筛选。

### 3.2 行为标注

- 视频播放器（播放/暂停/逐帧/倍速）。
- 时间轴标注：拖拽或按键标记行为起止区间。
- 行为类别选择：从预定义类别中单选或多选。
- 空间标注（可选）：播放器上叠加透明画布，支持手动拖拽矩形框记录局部区域。仅在标注者主动开启时显示。

### 3.3 片段导出

- 按标注区间调用 ffmpeg 裁剪导出视频片段。
- 片段按行为类别分目录存放。
- 同步导出统一事件格式 JSON。

### 3.4 协作与审核

- **用户角色**：管理员、标注者、审核者。
- **分工标注**：数据集按视频或按时间段分配给不同标注者，避免重复劳动。
- **审核流程**：标注者提交 → 审核者通过/退回 → 标注者修改后重新提交。
- 标注进度统计：按视频、按标注者、按类别展示完成率和审核通过率。

## 4. 数据模型概要

| 实体 | 关键字段 |
|---|---|
| 视频（Video） | id，文件名，时长，帧率，分辨率，上传时间 |
| 用户（User） | id，用户名，角色（admin/annotator/reviewer） |
| 标注任务（Task） | id，视频 id，标注者 id，分配的时间段，状态 |
| 行为标注（Annotation） | id，视频 id，标注者 id，行为类别，开始/结束时间，开始/结束帧，裁剪区域（可选），确定度，提交状态 |
| 审核记录（Review） | id，标注 id，审核者 id，结果（通过/退回），意见 |

## 5. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | Python FastAPI | 与现有 lab 环境一致，异步，文件处理强 |
| 数据库 | SQLite | 零配置，小团队足够，后续可换 PostgreSQL |
| 前端框架 | React | 视频标注交互丰富，社区成熟 |
| 视频播放 | Video.js | 成熟稳定，支持时间轴操作和插件扩展 |
| 视频裁剪 | ffmpeg 子进程 | 服务端按区间导出片段 |
| 文件存储 | 本地文件系统 | 视频和导出片段按目录组织 |

## 6. 非功能约束

- **部署**：单机部署，浏览器访问。
- **性能**：3–5 人同时标注时不卡顿；视频上传和片段导出可异步后台执行。
- **扩展性**：前端视频播放器预留叠加层接口，后续可插拔 YOLO 检测框、骨架、鼠标绘制的空间标注等图层。

## 7. 待定问题

| 编号 | 问题 | 影响 | 当前处理 |
|---|---|---|---|
| Q1 | 是否导入带 YOLO 标注痕迹的辅助视频 | 播放器需支持透明叠加层 | 前端预留叠加层架构，后续可加 |
| Q2 | 是否需要标注鼠框（精确到个体） | 标注交互和数据模型变更 | 当前不实现，不影响现有设计 |
| Q3 | 行为类别字典是否沿用 v0.1 术语表 | 前端类别列表来源 | 先用 v0.1 导入，后续可配置化 |
| Q4 | 视频和导出片段的存储路径和空间规划 | 服务器硬盘分配 | 待部署环境确认后确定 |

## 8. 下一步

1. 需求确认后，编写项目 README 并搭建前后端骨架。
2. 优先实现：视频上传与播放 + 时间轴标注 + 行为类别选择 + 片段导出。
3. 第二期实现：协作分工与审核流程。
