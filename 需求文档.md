# 数据标注网站需求文档

> 版本：v0.8
>
> 日期：2026-08-15
>
> 术语基线：[项目术语表](项目术语表.md)
>
> 现行架构：[检测状态、提交审核与独立行为视频片段导出设计](docs/设计/检测状态、提交审核与独立行为视频片段导出设计.md)

## 1. 目标与范围

本项目是多小鼠行为识别管线中的人工标注环节。系统接收顶视原始视频和 YOLO Pose/Tracker 逐帧结果，支持多人在线创建、审核并导出包含参与对象的行为标注。tracker 产生的 track ID 仅在一个视频的一次检测结果导入内有效，不表示跨视频生物身份。

不在当前范围：跨视频个体识别、自动 actor/target 推断、站内 YOLO 训练、检测框或关键点人工修改。

## 2. 输入与标注

### 2.1 三文件导入批次

- 原始视频。
- `tracks.jsonl`：逐帧检测框、tracker ID、关键点和置信度。
- `metadata.json`：视频尺寸、FPS、帧数、模型、tracker、关键点和骨架定义。

三个文件可分别上传和重试。视频可先播放并保存草稿；检测结果未校验可用时，行为标注可处于 `needs_mouse_ids`，但不得提交审核或进入正式导出。烧录框、track ID 或骨架的调试视频不作为正式输入。

### 2.2 行为标注

每条行为标注包含标注区间、行为类别、参与对象、可选 `crop_region` 和确定度。`mouse_ids` 是历史兼容字段名，保存指定检测结果快照下修正后 track ID 的去重有序数组，顺序不表达 actor/target。

当前默认 12 类：奔跑、行走、静止各恰好 1 个对象；一起、接近、追逐、回避、攻击行为、鼻头接触、鼻尾接触各恰好 2 个对象；扎堆行为至少 2 个对象；孤立行为恰好 1 个对象。类别是项目数据，不是前端硬编码常量。

track 修正提供 `Split`、`Merge`、“忽略整个 track”和 LIFO undo。原始文件与 `RawDetection` 保持不可变；当前 draft 状态由 sparse override 表达。旧 `CorrectedTrack`、CDA、`IdentityEdit`、`DetectionSuppression` 和 `SuppressionDetection` 仅保留迁移兼容，不是当前 authority，背景见[历史设计](docs/历史/YOLO检测结果接入与track修正设计.md)。

## 3. 提交与审核

- draft 编辑使用检测结果导入的 `edit_version` 进行并发校验；过期写入返回冲突，不得静默覆盖。
- `submit` 创建不可变 `Submission + DetectionSnapshot + SubmissionAnnotation`；提交和审核读取同一快照。
- `submitted` 锁定 draft 编辑；未裁决提交可 withdraw；rejected 后可继续编辑；新的 approved Submission 会将旧批准标记为 superseded，但不修改旧快照。
- `Review` 绑定 `Submission`。`Video.workflow_status` 和 `Annotation.review_status` 是兼容投影，不得反向成为审核或导出 authority。
- 视频工作流状态为 `draft/submitted/approved/rejected`；行为标注审核状态为 `pending/approved/rejected`，两套状态不得混用。
- 所有关键写入使用短事务、CAS 与数据库约束；ffmpeg、ffprobe 和 ZIP 操作在事务外执行。

## 4. 正式导出契约

正式项目 ZIP 只读取入队时冻结的 approved Submission authority。每个 `SubmissionAnnotation` 对应一个独立目录，固定且仅包含：

```text
视频片段目录/
├── clip.mp4
├── annotation.json
├── tracks.json
└── metadata.json
```

- `clip.mp4`：按行为帧范围精确重编码的 H.264 MP4。
- `annotation.json`：该片段的纯净行为数据。
- `tracks.json`：从 0 开始逐帧列出全部未抑制可见对象及最终 display track ID，空帧保留。
- `metadata.json`：clip、tracks、pose 与 source 定位元数据。
- crop 后检测框和关键点均使用 `clip_pixels` 坐标。
- 四文件须通过 schema、业务与 ffprobe 一致性校验后才允许原子发布整个 ZIP。
- 正式 ZIP **不包含**集中式 `annotations.json`、顶层 manifest、`corrected_tracks/` 或集中索引。

单视频 `/annotations/export` 继续提供 legacy 兼容 ExportEvent JSON；其中 `clip_file` 可为空。该接口不是正式项目 ZIP 契约。

## 5. 页面与角色

核心页面包括登录、项目入口、视频库、标注工作台、审核工作台、片段库、导出和项目管理。当前项目角色为 `owner/admin/annotator/reviewer`。

分工标注模块尚未实现。计划只按完整视频保存一个可空负责人，增加领取、改派和统计，并规划角色/审核能力调整；需求摘要仅到此，唯一详细 owner 为[分工标注模块需求文档](docs/计划/分工标注模块需求文档.md)。

## 6. 非功能约束

- 3–5 人协作规模；视频上传、媒体生成与导出采用后台任务。
- SQLite、媒体 worker、导出 worker 和清理 worker 当前按单应用进程部署。
- 原始检测、提交快照和已冻结 authority 受不可变约束保护。
- 受控存储 key、SHA-256、文件 identity 和 staging 隔离共同防止媒体路径替换竞态。
- 运行时数据不进入 Git；生产凭据、证书和私钥不进入仓库。

## 7. 当前证据与边界

- 此前媒体修复后端全量测试为 `399 passed, 8 skipped`。2026-08-17 在 Python 3.11.9 隔离环境中，确认 `.venv\Scripts` 已提供 imageio-ffmpeg 0.6.0 内置的 FFmpeg `7.1-essentials_build-www.gyan.dev`（含 libx264）和 npm `@ffprobe-installer/win32-x64@5.1.0` 的 ffprobe 5.1 兼容包。
- 真实集成测试为 `5 passed, 1 warning in 2.41s`，媒体与项目导出聚焦回归为 `66 passed, 1 warning in 51.90s`，均无 skip；warning 为既有 Starlette/httpx deprecation warning。已覆盖 25/30/60 FPS、H.264、yuv420p、300x200 crop、各 10 帧、约 `10/fps` 时长、JPEG 300x200、成功后无 `.part`/`.staging`、缩略图失败不发布，以及 MediaWorker 的 Job/Clip 更新为 `succeeded`/`ready`。
- 真实小鼠三文件 E2E 仅通过公开 API 完成：3.54 MB MOV、约 1.69 MB tracks JSONL 与 30 FPS/156 帧 metadata 成功导入 1877 条检测，真实 track 的帧 0–14 标注完成提交、审核通过、异步媒体 ready 与项目导出。116603 bytes ZIP 的片段目录严格符合独立四文件契约且 JSON/计数一致；MP4 为 H.264、yuv420p、2044×1080、15 帧、0.5 秒，且无 `.part`/`.staging`。
- 生产服务器 FFmpeg/ffprobe 4.4.2 对当前修复的兼容性仍未验证，不构成生产候选验收；`fix/clip-transcoding@bcc9a03` 未合并 `main`、未部署。
- 本地前端 npm 依赖与 production build 已通过；本次 E2E 使用的 ignored 配置和产物不是仓库提交，临时后端已停止并释放 8000 端口；仍应在目标浏览器持续做交互回归。
- systemd、服务器本机 HTTPS、SSH 隧道和实际上传已有验收证据，但不等于公网验收。
- 多地区 HTTPS 已成功，HTTP 及部分来源仍受备案同步影响，完整公网验收待完成；不得写成完全不可用或完全上线。实时服务器事实由仓库外网站服务器清单维护。

## 8. 后续计划摘要

- 分工标注：尚未实现，详见[专项计划](docs/计划/分工标注模块需求文档.md)。
- 前端键盘快捷键：数字类别映射、参与对象导航和相邻视频导航已实现；下一轮 `Tab`/`T`/`Enter` 调整尚未实现，全部细节与待确认项只在[快捷键计划](docs/计划/前端全键盘快捷键优化方案.md)维护。
- 播放与带宽优化：HTTP Range 流式、低码率播放副本、缓存、前端加载策略和按需异步生成五类优化均尚未实施，详见[播放与带宽优化计划](docs/计划/播放与带宽优化计划.md)。
- 备份恢复演练、长视频与多人压力验证、HTTP 及剩余公网入口的备案同步复验仍需完成。
