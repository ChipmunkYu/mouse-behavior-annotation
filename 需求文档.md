# 数据标注网站需求文档

> 版本：v0.7
> 日期：2026-08-10
> 对应工作项：WI-20260805-22
> 术语基线：`项目术语表.md`（本文须遵循该权威定义）
> 分工标注模块：详细计划规格见 `分工标注模块需求文档.md`（WI-20260810-24，尚未实现）

## 1. 项目背景

本项目处于小鼠行为识别阶段。上游 YOLO Pose/Tracker 已能提供逐帧检测框、关键点和 track ID（技术输入中的 tracker ID），但 track ID 会因遮挡、漏检和交换而断裂或错误，不能直接视为长期可靠身份。本阶段的最终目标是从多小鼠顶视视频中提取包含参与对象、可审核且可回溯的行为事件。

标注网站是行为识别管线中的人工标注环节，替代原先的 BORIS 桌面单机方案，提供**多人在线协作标注与审核**能力。

## 2. 标注场景

### 2.1 输入

- **原始视频**：顶视角群养小鼠长视频。
- **YOLO 逐帧结果**：与视频匹配的 `tracks.jsonl`，包含逐帧检测框、tracker ID、关键点和置信度。
- **检测元数据**：`metadata.json`，包含视频尺寸、FPS、帧数、模型、tracker、关键点和骨架定义。
- 三者组成**三文件导入批次**并配对校验，但允许分别上传和重试。视频上传成功后即可播放和暂存标注区间；检测结果未校验可用时，不得提交审核或进入正式导出。
- 不接收把框、track ID 或骨架烧录进像素的辅助视频作为正式输入。原始结构化文件只读留存，标注和导出使用人工修正后的 track ID。

### 2.2 标注操作

1. 标注者在视频时间轴上标记行为片段的**开始时间和结束时间**。
2. 为片段指定**行为类别**（多分类，具体类别由行为字典定义）。
3. 从当前帧检测框中选择该行为的**参与对象**。`mouse_ids` 是历史兼容字段名，语义与目标种类无关；其值是当前视频、当前检测结果导入修订下的修正后 track ID 数组，并直接内嵌保存到行为标注。
4. 如发现 track ID 丢失、断裂或交换，标注者先在 track 修正模式中执行 `Split` / `Merge`；确认整条 track 均为误检时使用“忽略整个 track”。这些操作可撤销、可审计，原始检测保持不可变，不执行物理删除。
5. 可选：在视频画面上手动拖框，裁切出发生行为的**局部区域**。`crop_region` 是媒体裁剪区域，不代表参与对象身份。
6. 正式提交前，服务端按行为类别的 `mouse_count_min`、`mouse_count_max` 规则校验 `mouse_ids` 数量、去重、视频归属、检测结果导入修订和 track 修正修订。

### 2.3 输出

- 按标注区间**导出视频片段文件**（服务端 ffmpeg 裁剪）。
- **统一事件格式 JSON**，兼容现有行为识别下游：
 ```json
 {
 "annotation_id": 123,
 "video_id": "video_01",
 "clip_file": "clips/社交行为/攻击行为/clip_123.mp4",
 "start_time": 12.4,
 "end_time": 14.84,
 "start_frame": 310,
 "end_frame": 371,
 "behavior": "attack",
 "mouse_ids": [8, 20],
 "detection_import_revision": 1,
 "identity_revision": 7,
 "crop_region": null,
 "confidence": "certain",
 "annotator": "annotator_01",
 "reviewer": null,
 "review_status": "pending"
 }
 ```

> `mouse_ids` 直接内嵌在导出的行为事件中并按数值升序规范化；列表顺序不表达 actor/target。每个值都是当前视频、指定检测结果导入修订下的修正后 track ID，不表示跨视频生物身份。事件的核心训练标注为 `(mouse_ids, start_frame, end_frame, behavior)`。
>
> 单视频 `/annotations/export` 返回上述完整 ExportEvent 字段；没有 ready Clip 时 `clip_file` 可为 `null`。项目 ZIP 中的 `clip_file` 则必须是安全非空的 ZIP 相对路径。

### 2.4 初始行为类别

项目创建时，依据 `行为识别/data/北医标注-行为例子/` 的文件夹结构初始化以下 12 个类别。它们是默认数据而非代码中的固定常量；方向性社交行为退化为无向标签（标注时不区分主动方/被动方）。每个类别同时保存对象数量约束。

| 一级分组 | 二级标签 | 对象数量 |
|---|---|---:|
| 个体行为 | 奔跑、行走、静止 | 恰好 1 个 |
| 社交行为 | 一起、接近、追逐、回避、攻击行为、鼻头接触、鼻尾接触 | 恰好 2 个 |
| 群体行为 | 扎堆行为 | 至少 2 个，无固定上限 |
| 群体行为 | 孤立行为 | 恰好 1 个 |

## 3. 核心功能

### 3.1 视频管理

- 上传原始视频，记录视频元信息（文件名、时长、帧率、分辨率）。
- 在同一三文件导入批次中识别并配对原始视频、`tracks.jsonl` 和 `metadata.json`；三个文件可独立上传、重试和校验。
- 已有视频支持补传或替换 YOLO 数据。替换会创建新的检测结果导入修订，不静默复用旧 track 修正或事件中的 `mouse_ids`。
- 列表浏览已上传视频，支持搜索和筛选。
- 视频库状态筛选严格使用视频 `workflow_status`，不使用媒体 `status` 或单条 `Annotation.review_status`。
- 三文件批次首次创建视频并完成检测导入时，从 metadata 同步 FPS、宽、高和 `duration=frame_count/fps`；`source_relative` 按 basename 与视频文件名匹配。已有视频替换检测结果时校验 source basename、FPS、宽、高和当前导入 `frame_count`。预览请求和任一失败路径都会清理候选 tracks/metadata 文件，只有 `confirm=true` 且事务成功才保留新文件。

### 3.2 行为标注

- 视频播放器（播放/暂停/逐帧/倍速）。
- 时间轴标注：拖拽或按键标记行为起止区间。
- 行为类别选择：通过 API 读取当前项目启用的类别；每条 Annotation 选择一个类别，需要并行表达多个类别时创建多条可重叠事件。
- 参与对象选择：叠加当前帧检测框和修正后 track ID，点击框或列表项切换选择；选中结果作为 `mouse_ids` 直接保存到行为标注。界面实时显示当前数量与类别允许范围。
- 没有检测结果导入时仍可创建 `needs_mouse_ids` 草稿；前端省略 `mouse_ids` 和检测结果导入/track 修正修订。此类草稿不能提交审核或进入正式导出，导入成功后必须补选参与对象。
- track 修正：对单选 track 执行 `Split`，对多选 track 执行 `Merge`；确认整条 track 均为误检时使用“忽略整个 track”，技术上属于整轨检测抑制。原始检测保持不可变，全部修正可撤销、可审计。
- 空间标注（可选）：播放器上叠加透明画布，支持手动拖拽矩形框记录局部区域。仅在标注者主动开启时显示。

### 3.3 片段导出

- 按标注区间调用 ffmpeg 裁剪导出视频片段。
- 片段按行为类别分目录存放。
- 同步导出包含内嵌 `mouse_ids` 和修订快照的统一事件格式 JSON。
- 正式项目 ZIP 为独立片段契约：每个片段目录恰含 `clip.mp4`、`annotation.json`、`tracks.json`、`metadata.json`，不生成集中式 `annotations.json`、manifest 或 `corrected_tracks/`。
- 单视频 `/annotations/export` 是单独的 legacy 兼容 JSON API，不代表正式项目 ZIP 结构。

### 3.4 协作与审核

- **用户与项目身份分离**：用户账号本身不携带业务角色；用户创建或加入项目后，才通过项目成员关系获得该项目内的身份。
- **项目内身份**：当前实现采用 `owner/admin/annotator/reviewer`。分工模块计划简化为 `owner/admin/member`，owner/admin 默认具备审核能力，member 通过 `ProjectMembership.can_review` 获得审核能力；该计划尚未实现。
- **分工标注**：首版计划仅按完整视频保存一个可空的当前负责人，用于责任归属、查找和数量统计，不隔离编辑权限，不分配时间段，也不新增独立任务状态。详细规则见 `分工标注模块需求文档.md`。
- **审核流程**：标注完成后提交整个视频审核，由具备审核能力的成员查看标注区间、行为标注内嵌 `mouse_ids` 及修正后 track，并执行通过/退回；允许审核自己负责或参与标注的视频。审核快照同时锁定行为标注修订、检测结果导入修订和 track 修正修订；媒体修订单独管理，不在审核锁定范围内。
- **状态区分**：视频 `workflow_status` 使用草稿/待审核/已通过/已退回（`draft/submitted/approved/rejected`）；单条 `Annotation.review_status` 独立使用 `pending/approved/rejected`，不含 `draft/submitted`。
- **提交与通过校验**：`submit` 以及 approval 前的 `_revalidate_annotations` 均为纯校验，不在发现问题时写 Annotation；提交语义无效返回 400、通过审核前语义无效返回 409，数据库均保持不变。全部语义校验通过后，服务端才在同一成功事务内将已验证 Annotation 置为 `valid` 并同步到当前检测结果导入/track 修正修订，再提交 `workflow_status=submitted` 或完成通过审核。已存修订 stale 但语义仍有效的标注允许在该成功事务内推进。
- **track 修正失效范围**：Split、Merge、对应撤销、整轨检测抑制及其撤销后，服务端重校验该视频全部 Annotation；有效项更新到新检测结果导入/track 修正修订，无效项进入 `needs_mouse_ids`。仅实际受影响且原为 `approved` 的单条标注改为 `pending`；视频处于 `submitted/approved` 时退回 `draft`，`rejected` 不会因这些操作被笼统重置为 `draft`。
- **撤销边界**：`GET .../detection-suppressions` 只列当前 active import 中尚未撤销的抑制，页面刷新后仍可恢复整轨 suppression 撤销入口；旧 import 的 suppression 撤销返回 409。当前页面会话内可按实际完成顺序统一撤销具有可靠操作 ID 的 Split、Merge 与整轨忽略；刷新后不恢复这条统一历史，既有整轨 suppression 仍可通过记录旁入口单独撤销。
- 分工模块首版进度统计仅按成员统计负责视频数及 `workflow_status` 数量，并单列未分配数；详细口径见 `分工标注模块需求文档.md`。

## 4. 数据模型概要

| 实体 | 关键字段 |
|---|---|
| 用户（User） | id，用户名，密码哈希 |
| 项目（Project） | id，名称，创建者 id，创建时间，状态 |
| 项目成员（ProjectMembership） | 当前：id，project_id，user_id，role（owner/admin/annotator/reviewer），成员状态，加入时间；分工模块计划：role 简化为 owner/admin/member，并增加 can_review |
| 行为类别（BehaviorCategory） | id，project_id，名称，分组，颜色，排序，是否启用，mouse_count_min，mouse_count_max |
| 视频（Video） | id，project_id，文件名，时长，帧率，分辨率，上传时间；分工模块计划增加可空的当前负责人字段 |
| 检测导入（DetectionImport） | id，video_id，修订号，tracks/metadata 路径与校验和，schema/model/tracker 信息，状态，帧统计 |
| 原始检测（RawDetection） | id，detection_import_id，frame_index，raw_track_id，框、关键点、置信度；导入后不可变 |
| 修正后 track（CorrectedTrack） | id，detection_import_id，display_track_id，首次/末次帧，状态，被合并目标；仅表示该视频内修正后的 YOLO track |
| track 修正操作（IdentityEdit） | id，video_id，基础/结果 track 修正修订，`Split`/`Merge` 类型，参数，操作者，时间 |
| 检测抑制（DetectionSuppression） | id，video_id，track 修正修订，整轨作用域及冻结的 detection 集合，操作者，撤销状态 |
| 行为标注（Annotation） | id，视频 id，标注者 id，行为类别 id，mouse_ids（修正后 track ID 的去重有序 JSON 数组），开始/结束时间与帧，检测结果导入/track 修正修订，裁剪区域（可选），确定度，mouse_id_status，行为标注修订 |
| 审核记录（Review） | id，视频/标注范围，审核者 id，结果，意见，annotation_revision，detection_import_revision，identity_revision |

> **身份设计决策**：User 只表示独立登录账号。创建项目时，创建者自动获得该项目的 `owner` 身份；加入项目时，通过 ProjectMembership 获得项目内身份。`user_id + project_id` 应保持唯一，所有视频分工和权限判断都限定在当前项目内。分工模块不新增独立 Task 实体。

## 5. 页面架构

共 6 个核心页面，按“登录 → 选择项目 → 标注”工作流组织。P1 实现登录、项目入口、视频库和标注工作台；P2 完善审核和项目管理后台。

### 导航结构

```
顶部导航栏（登录后始终可见）
├── 当前项目切换器
├── 视频库
├── 标注工作台（当前打开时高亮）
├── 审核工作台（当前按审核角色/管理员控制；分工模块计划按审核能力控制）
├── 项目管理（仅项目所有者/管理员可见，P2）
└── 用户头像 → 退出登录
```

### 5.1 登录页 `/login`

| 区块 | 功能 |
|---|---|
| 登录表单 | 用户名 + 密码登录 |

### 5.2 项目入口 `/projects`

| 区块 | 功能 |
|---|---|
| 我的项目 | 展示当前用户已经加入的项目及其项目内身份 |
| 创建项目 | 创建项目，并将创建者登记为项目所有者 |
| 加入项目 | 当前代码不支持成员自助加入或邀请码流程；分工模块计划新增项目单一邀请码，用户输入有效邀请码后以 member 身份加入，详细规则见 `分工标注模块需求文档.md` |

### 5.3 视频库 `/projects/:projectId/videos`

| 区块 | 功能 |
|---|---|
| 顶部操作栏 | 上传视频、搜索、按视频 `workflow_status` 筛选；分工模块计划增加“我的任务 / 待领取 / 全部”和负责人筛选 |
| 视频列表 | 卡片式，显示缩略图、文件名、时长、标注进度；分工模块计划增加当前负责人、领取和批量分配入口 |
| 操作 | 点击进入标注工作台 |

### 5.4 标注工作台 `/projects/:projectId/annotate/:videoId`

| 区块 | 功能 |
|---|---|
| 视频播放器（主区域） | 播放/暂停/逐帧/倍速；叠加透明画布（预留）；当前帧时间显示 |
| 时间轴（播放器下方） | 已标注片段着色显示；拖拽标记起止点；缩放时间精度 |
| 行为类别面板（右侧栏） | 当前项目启用的类别按分组显示为按钮；选中后开始标注 |
| 参与对象面板 | 显示 `mouse_ids` 和类别允许的数量范围；视频框和列表同步多选；支持清空、移除和继续添加 |
| track 修正模式 | 与行为标注模式互斥；提供当前帧/全部 track、搜索、Split、Merge、忽略整个 track、撤销和历史 |
| 行为标注列表（右侧栏下方） | 当前视频的行为标注列表，支持编辑时间/类别、删除、提交 |
| 状态栏（底部） | 当前操作提示、片段起止时间实时显示 |

**标注流程**：播放视频 → 标记行为起止与类别 → 在清晰帧选择参与对象 → 必要时进入 track 修正模式修复 track ID → 服务端校验 `mouse_ids` 和三类审核修订 → 将 track ID 列表直接写入行为标注并提交审核。

### 5.5 审核工作台 `/projects/:projectId/review`（P2）

| 区块 | 功能 |
|---|---|
| 待审列表（左侧） | 按视频/标注者分组，显示待审核数量 |
| 审核播放器（主区域） | 与标注工作台相同，只读展示行为标注、参与对象、修正后 track ID 和检测框 |
| 审核操作栏（右侧） | 行为标注、`mouse_ids` 及修订信息 + 通过/退回按钮（退回需填写意见） |

### 5.6 项目管理后台 `/projects/:projectId/admin`（P2）

| 区块 | 功能 |
|---|---|
| 成员管理 | 当前按既有四角色管理；分工模块计划采用单一邀请码、owner/admin/member 与 member 级 `can_review`；成员仍负责视频时禁止移除，规则见专项文档 |
| 分工管理 | 按完整视频分配、改派或释放当前负责人，查看各成员任务数量；首版仅允许在 `draft/rejected` 阶段改派，`submitted/approved` 阶段禁止；不新增 Task 表或时间段分配 |
| 行为类别管理 | 增删改项目类别（名称、分组、颜色、排序）；已被标注引用的类别停用而不物理删除 |
| 导出中心 | 选择视频/时间范围批量导出片段 + JSON |

## 6. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | Python FastAPI | 与现有 lab 环境一致，异步，文件处理强 |
| 数据库 | SQLite | 零配置，小团队足够，后续可换 PostgreSQL |
| 前端框架 | React + TypeScript（Vite 脚手架） | 类型安全，HMR 开发体验好 |
| 视频播放 | Video.js | 成熟稳定，支持时间轴操作和插件扩展 |
| 视频裁剪 | ffmpeg 子进程 | 服务端按区间导出片段 |
| 文件存储 | 本地文件系统 | `data/videos/` 存原始视频，`data/exports/` 存导出片段 |

## 7. 非功能约束

- **部署**：单机部署，浏览器访问。
- **性能**：3–5 人同时标注时不卡顿；视频上传和片段导出可异步后台执行。
- **性能**：检测数据按帧区间读取并有限缓存，不把长视频全部检测一次载入浏览器。
- **可审计性**：原始 YOLO 文件和原始检测不可变；Split、Merge、抑制及撤销均保存操作者、时间、基础修订和影响范围。
- **一致性**：行为字段、内嵌 `mouse_ids` 和修订校验使用同一事务；并发编辑基于修订号冲突返回，不允许静默覆盖。

## 8. 待定问题

| 编号 | 问题 | 影响 | 当前处理 |
|---|---|---|---|
| Q1 | 是否表达 actor/target 等方向性角色 | 影响行为语义与导出 | 当前 12 类保持无向；`mouse_ids` 顺序无语义，后续如调整行为字典再新增独立字段 |
| Q2 | 长视频分段查询与缓存大小 | 影响浏览器性能 | 实现阶段通过真实长视频压测确定，不改变数据契约 |
| Q3 | 行为类别字典来源 | ~~前端类别列表来源~~ | ✅ 已确定：按北医目录初始化 12 个项目级类别；P1 通过 API 读取，P2 提供管理界面 |
| Q4 | 视频和导出片段的存储路径 | ~~服务器硬盘分配~~ | ✅ 已确定：P1 本地默认路径为 `backend/data/videos/`（视频）与 `backend/data/exports/`（导出片段），运行时数据已 gitignore；网站服务器生产部署使用 `DATA_DIR=/data/mouse-annotation/data` |

## 9. 当前实现状态与下一步

### 9.1 当前已实现

- `feature/spatial-annotation` 已在生产扩展基线上实现三文件导入批次与配对校验、检测框/关键点/骨架叠加、行为标注内嵌 `mouse_ids`、Split/Merge、整轨检测抑制与撤销、三类修订审核、修正后 track 结果 `tracks.corrected.jsonl`，以及项目 ZIP 中基于 `clip_file` 的集中索引和双向完整性校验。
- 旧无参与对象 Annotation 已按 v0.6 迁移为待补 ID 状态，不再作为正式审核或导出结果。
- 演示数据由 `backend/scripts/seed_demo.py` 提供（demo/demo123 仅开发用）；启动与测试说明见 `backend/README.md`、`frontend/README.md`。

### 9.2 剩余范围

- 真实长视频、真实 ffmpeg、浏览器完整流程和生产部署仍需人工验收；当前不声称这些环节已完成。
- 功能尚未合并到 `main`，需在验收后决定提交、推送与合并。

### 9.3 下一步

1. 使用正式原始视频 `社交-攻击1.mov` 完成上传、叠加、track 修正、标注、审核、真实 ffmpeg 与导出的端到端人工验收；烧录调试视频 `社交-攻击1_all_ids.mp4` 不作为正式输入。
2. 补充长视频浏览器性能、多人并发和生产部署验收。
3. 根据验收结果决定提交、推送并合并到 `main`。

## 10. 后续优化与实现边界

### 10.1 行为类别快捷键

- 当前已实现数字键选择行为类别：按 `sort_order`、`id`稳定排序后的前 10 个启用行为类别映射到 `1`–`9`、`0`，第 11 个及以后继续使用按钮。
- 当前实现、映射边界与已验证状态以 `前端全键盘快捷键优化方案.md`为准；后续键位调整由 UI 优化分支单独规划和验收。
- **下一轮待实现计划摘要**：裸 `Tab`在行为标注与 track 修正间切换，`T`显式进入/退出当前模式的 track 列表键盘导航，导航中用`↑` / `↓`移动高亮、`Enter`选择或取消；`Space`只保留视频播放/暂停，确认弹窗改为`Enter`确认、`Esc`取消。`Shift+Tab`如何处理及模式切换时的状态清理策略仍需在实现前确认。
- 上述下一轮调整尚未修改代码；详细保护边界、原生按钮和焦点处理、无 detection import、长按重复及跨页面 Space 审计验收项见 `前端全键盘快捷键优化方案.md`第 1.3 节。

### 10.2 真实视频上传边界（已实现）

- 视频库已有真实文件上传入口，接受 MP4、MOV、AVI、MKV、WebM、M4V 等常见视频格式。
- 应用层不设置固定文件大小上限；上传采用流式写入，并在开始和写入过程中检查可用磁盘空间。
- 服务器、反向代理和磁盘容量仍构成实际限制，不能将“无应用大小上限”理解为无限容量。
- 浏览器无法直接播放的编码格式允许上传并标记为待转码；实际转码仍是后续能力。

### 10.3 裁剪模式

| 模式 | 特点 | 适用场景 |
|---|---|---|
| 快速无损裁剪（stream copy） | 不重新编码，速度快、CPU 占用低；起止点受关键帧影响，可能不精确，且不会改善原编码兼容性 | 快速预览、边界精度要求不高 |
| 精确重编码 | 按准确起止时间解码并重新编码；速度较慢、占用 CPU，但可统一输出 H.264 MP4 | 行为识别训练片段、正式导出 |

已确认正式片段采用**精确重编码**，统一输出浏览器兼容的 H.264 MP4；不要求 GPU。

### 10.4 标注、审核与片段生命周期

```text
draft（可修改）
 → 提交审核
submitted
 → 审核通过
approved
 → 后台生成实体行为片段与缩略图
```

- 仅审核通过的标注进入实体片段生成和项目导出范围。
- 审核通过后仍允许修改；时间、空间裁剪或源视频变化时，审核失效并重生成实体 Clip 与缩略图。
- 类别、`mouse_ids`、检测结果导入或 track 归属变化时，审核及导出元数据失效，但时间和像素内容未变化的 Clip 不重新执行 ffmpeg。
- 实现时需拆分语义修订和媒体内容修订，避免现有视频级粗粒度修订造成无谓重编码。

> 上述状态机是视频工作流；单条行为标注的 `review_status` 独立使用 `pending/approved/rejected`。审核锁定 `annotation_revision`、`detection_import_revision`、`identity_revision`，而 `media_revision` 单独决定视频片段重编码。

### 10.5 跨视频行为片段库

- 新增项目级行为片段库，跨视频按行为类别展示审核通过的片段。
- 每个片段需要缩略图、源视频名称、行为类别、起止时间、时长和审核状态。
- 列表不同时加载全部源视频；点击片段后使用共享播放器预览，并可跳回原视频标注位置。

### 10.6 全项目分类导出

- 仅导出审核通过且实体片段状态正常的标注。
- 导出前补生成缺失片段，按当前行为分组与类别组织目录，同时写出 `annotations.json`，最后生成 ZIP。
- `annotations.json` 是本次导出包的集中片段索引：一条记录对应一个实际 MP4，使用 `clip_file` 保存规范化、安全、相对于 ZIP 根目录的路径；项目级导出可同时索引多个源视频。
- 裁剪和打包均使用后台任务，原型阶段限制为单任务执行。

### 10.7 建议保留时间

| 数据 | 建议保留时间 |
|---|---|
| 原始上传视频 | 不自动删除，由项目所有者明确清理 |
| 审核通过的实体片段与缩略图 | 与有效审核标注同步长期保留；标注修改或删除时立即清理旧版本 |
| 导出 ZIP | 生成后保留 7 天 |
| 未完成上传、裁剪临时文件 | 24 小时 |
| 失败任务临时文件 | 24 小时，保留错误记录供排查 |
| 后台任务日志 | 30 天 |

以上均为默认建议，部署时可通过配置调整。

### 10.8 YOLO track 修正实施顺序

以下实施序列已在 `feature/spatial-annotation` 完成：

1. ✅ 数据迁移、检测导入与修正后 track 模型；
2. ✅ 三文件配对校验和按帧查询；
3. ✅ 检测叠加及 Annotation 内嵌 `mouse_ids` 保存；
4. ✅ Split、Merge、整轨误检抑制、撤销与审计；
5. ✅ 三类审核修订快照、细粒度媒体失效和旧标注迁移；
6. ✅ 新版行为事件 JSON、修正后 track 结果与项目 ZIP 导出，包括 `clip_file` 生成、路径安全检查、行为事件/MP4 双向完整性校验及单视频/多视频/筛选导出测试；
7. ⏳ 真实长视频、浏览器、真实 ffmpeg 与生产部署人工验收。

自动化证据：后端全量 `301 passed, 3 skipped, 1 warning`；前端 `npm run build` 通过。

### 10.9 已完成生产扩展批次

以下批次已经在 `main` 完成，作为 YOLO 身份能力的现有基线：

| 批次 | 提交主题 | 主要内容 | 验收门槛 |
|---|---|---|---|
| 0 | 文档基线 | 确认精确重编码、审核后生成、保留时间与实施边界 | 仅文档变更，不包含运行时导出文件 |
| 1 | 数据模型与迁移 | 引入数据库迁移；增加视频工作流状态、审核记录、Clip、后台任务与修订字段 | 旧数据库可迁移；状态与外键测试通过 |
| 2 | 真实视频上传 | 流式上传、常见格式校验、安全文件名、磁盘空间检查、上传进度与视频库入口 | 大小不设应用固定上限；上传后可播放或明确显示待转码 |
| 3 | 提交与审核闭环 | `draft → submitted → approved/rejected`；审核页面；当前基线在修改已审核标注时粗粒度退回 draft 并删除 Clip，身份阶段须按 §10.4 拆分语义修订与媒体修订 | 状态、权限、退回与清理测试通过 |
| 4 | 实体片段与缩略图 | 审核通过后创建后台任务，使用 ffmpeg 精确重编码 H.264 MP4，并生成缩略图；失败可重试 | 边界准确、修订隔离、失败恢复和单任务并发测试通过 |
| 5 | 跨视频行为片段库 | 项目级分类、筛选、分页、缩略图、共享播放器预览、跳回源标注 | 不批量加载完整视频；仅展示有效审核片段 |
| 6 | 全项目分类导出 | 补生成缺失片段，按类别组织目录，写出集中 `annotations.json`，每条记录携带 `clip_file`，后台生成 ZIP | 只含审核通过片段；每个 MP4 与事件双向一一对应；路径安全且可下载 |
| 7 | 保留与清理 | ZIP 7 天、临时文件 24 小时、任务日志 30 天；配置、清理任务和运维说明 | 清理不影响原视频和有效 Clip；可通过配置覆盖 |

新增 YOLO track 修正能力不得回退上述上传、审核、媒体、导出与清理行为；详细模型、API、迁移和验收标准以 `YOLO检测结果接入与track修正设计.md` 为准。
