# 数据标注网站需求文档

> 版本：v0.4
> 日期：2026-08-05  
> 对应工作项：WI-20260805-22

## 1. 项目背景

本项目处于小鼠行为识别阶段。上游已完成 YOLO 姿态估计与跨帧追踪，获得每只小鼠的长期身份、位置和关键点信息。本阶段的最终目标是从多小鼠顶视视频中提取可回溯的社会行为事件。

标注网站是行为识别管线中的人工标注环节，替代原先的 BORIS 桌面单机方案，提供**多人在线协作标注与审核**能力。

## 2. 标注场景

### 2.1 输入

- **原始视频**：顶视角群养小鼠长视频。
- 视频可能预先烧录 YOLO 检测框和身份 ID（待定，详见第 7 节）。

### 2.2 标注操作

1. 标注者在视频时间轴上标记行为片段的**开始时间和结束时间**。
2. 为片段指定**行为类别**（多分类，具体类别由行为字典定义）。
3. 可选：在视频画面上手动拖框，裁切出发生行为的**局部区域**。
4. 不标记小鼠个体身份。

### 2.3 输出

- 按标注区间**导出视频片段文件**（服务端 ffmpeg 裁剪）。
- **统一事件格式 JSON**，兼容现有行为识别下游：
  ```json
  {
    "video_id": "video_01",
    "start_time": 12.4,
    "end_time": 14.84,
    "start_frame": 310,
    "end_frame": 371,
    "behavior": "attack",
    "crop_region": null,
    "confidence": "certain",
    "annotator": "annotator_01",
    "reviewer": null,
    "review_status": "pending"
  }
  ```

> 各字段含义和约束详见 `行为识别/调研/行为标注工具调研.md` §5。

### 2.4 初始行为类别

项目创建时，依据 `行为识别/data/北医标注-行为例子/` 的文件夹结构初始化以下 12 个类别。它们是默认数据而非代码中的固定常量；方向性社交行为退化为无向标签（标注时不区分主动方/被动方）。

| 一级分组 | 二级标签 |
|---|---|
| 个体行为 | 奔跑、行走、静止 |
| 社交行为 | 一起、接近、追逐、回避、攻击行为、鼻头接触、鼻尾接触 |
| 群体行为 | 扎堆行为、孤立行为 |

## 3. 核心功能

### 3.1 视频管理

- 上传原始视频，记录视频元信息（文件名、时长、帧率、分辨率）。
- 列表浏览已上传视频，支持搜索和筛选。

### 3.2 行为标注

- 视频播放器（播放/暂停/逐帧/倍速）。
- 时间轴标注：拖拽或按键标记行为起止区间。
- 行为类别选择：通过 API 读取当前项目启用的类别，支持单选或多选。
- 空间标注（可选）：播放器上叠加透明画布，支持手动拖拽矩形框记录局部区域。仅在标注者主动开启时显示。

### 3.3 片段导出

- 按标注区间调用 ffmpeg 裁剪导出视频片段。
- 片段按行为类别分目录存放。
- 同步导出统一事件格式 JSON。

### 3.4 协作与审核

- **用户与项目身份分离**：用户账号本身不携带业务角色；用户创建或加入项目后，才通过项目成员关系获得该项目内的身份。
- **项目内身份**：项目所有者、管理员、标注者、审核者。同一用户可以在不同项目中拥有不同身份。
- **分工标注**：数据集按视频或按时间段分配给不同标注者，避免重复劳动。
- **审核流程**：标注者提交 → 审核者通过/退回 → 标注者修改后重新提交。
- 标注进度统计：按视频、按标注者、按类别展示完成率和审核通过率。

## 4. 数据模型概要

| 实体 | 关键字段 |
|---|---|
| 用户（User） | id，用户名，密码哈希 |
| 项目（Project） | id，名称，创建者 id，创建时间，状态 |
| 项目成员（ProjectMembership） | id，project_id，user_id，role（owner/admin/annotator/reviewer），成员状态，加入时间 |
| 行为类别（BehaviorCategory） | id，project_id，名称，分组，颜色，排序，是否启用 |
| 视频（Video） | id，project_id，文件名，时长，帧率，分辨率，上传时间 |
| 标注任务（Task） | id，project_id，视频 id，标注者 id，分配的时间段，状态 |
| 行为标注（Annotation） | id，视频 id，标注者 id，行为类别 id，开始/结束时间，开始/结束帧，裁剪区域（可选），确定度，提交状态 |
| 审核记录（Review） | id，标注 id，审核者 id，结果（通过/退回），意见 |

> **身份设计决策**：User 只表示独立登录账号。创建项目时，创建者自动获得该项目的 `owner` 身份；加入项目时，通过 ProjectMembership 获得被分配的项目身份。`user_id + project_id` 应保持唯一，所有视频、任务和权限判断都限定在当前项目内。

## 5. 页面架构

共 6 个核心页面，按“登录 → 选择项目 → 标注”工作流组织。P1 实现登录、项目入口、视频库和标注工作台；P2 完善审核和项目管理后台。

### 导航结构

```
顶部导航栏（登录后始终可见）
├── 当前项目切换器
├── 视频库
├── 标注工作台（当前打开时高亮）
├── 审核工作台（仅审核者/管理员可见，P2）
├── 项目管理（仅项目所有者/管理员可见，P2）
└── 用户头像 → 退出登录
```

### 5.1 登录页 `/login`

| 区块 | 功能 |
|---|---|
| 登录表单 | 用户名 + 密码登录 |

### 5.2 项目入口 `/projects`

| 区块 | 功能 |
|---|---|
| 我的项目 | 展示当前用户已经加入的项目及其项目内身份 |
| 创建项目 | 创建项目，并将创建者登记为项目所有者 |
| 加入项目 | 通过邀请或成员分配加入项目，并获得该项目内身份；具体邀请方式可在 P2 完善 |

### 5.3 视频库 `/projects/:projectId/videos`

| 区块 | 功能 |
|---|---|
| 顶部操作栏 | 上传视频、搜索、按标注状态筛选 |
| 视频列表 | 卡片式，显示缩略图、文件名、时长、标注进度 |
| 操作 | 点击进入标注工作台 |

### 5.4 标注工作台 `/projects/:projectId/annotate/:videoId`

| 区块 | 功能 |
|---|---|
| 视频播放器（主区域） | 播放/暂停/逐帧/倍速；叠加透明画布（预留）；当前帧时间显示 |
| 时间轴（播放器下方） | 已标注片段着色显示；拖拽标记起止点；缩放时间精度 |
| 行为类别面板（右侧栏） | 当前项目启用的类别按分组显示为按钮；选中后开始标注 |
| 标注段列表（右侧栏下方） | 当前视频已标注片段列表，支持编辑时间/类别、删除、提交 |
| 状态栏（底部） | 当前操作提示、片段起止时间实时显示 |

**标注流程**：播放视频 → 看到行为起点按类别按钮标记开始 → 行为终点再次点击标记结束 → 右侧自动生成标注段 → 可微调 → 提交。

### 5.5 审核工作台 `/projects/:projectId/review`（P2）

| 区块 | 功能 |
|---|---|
| 待审列表（左侧） | 按视频/标注者分组，显示待审核数量 |
| 审核播放器（主区域） | 与标注工作台相同，只读模式，标注段着色 |
| 审核操作栏（右侧） | 标注段信息 + 通过/退回按钮（退回需填写意见） |

### 5.6 项目管理后台 `/projects/:projectId/admin`（P2）

| 区块 | 功能 |
|---|---|
| 成员管理 | 邀请或移除项目成员、分配该项目内身份 |
| 任务分配 | 将视频分配给标注者；查看各人工作量 |
| 行为类别管理 | 增删改项目类别（名称、分组、颜色、排序）；已被标注引用的类别停用而不物理删除 |
| 导出中心 | 选择视频/时间范围批量导出片段 + JSON |

## 6. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | Python FastAPI | 与现有 lab 环境一致，异步，文件处理强 |
| 数据库 | SQLite | 零配置，小团队足够，后续可换 PostgreSQL |
| 前端框架 | React + TypeScript（Vite 脚手架） | 类型安全，HMR 开发体验好 |
| 视频播放 | Video.js | 成熟稳定，支持时间轴操作和插件扩展 |
| 视频裁剪 | ffmpeg 子进程 | 服务端按区间导出片段 |
| 文件存储 | 本地文件系统 | `data/videos/` 存原始视频，`data/exports/` 存导出片段 |

## 7. 非功能约束

- **部署**：单机部署，浏览器访问。
- **性能**：3–5 人同时标注时不卡顿；视频上传和片段导出可异步后台执行。
- **扩展性**：前端视频播放器预留叠加层接口，后续可插拔 YOLO 检测框、骨架、鼠标绘制的空间标注等图层。

## 8. 待定问题

| 编号 | 问题 | 影响 | 当前处理 |
|---|---|---|---|
| Q1 | 是否导入带 YOLO 标注痕迹的辅助视频 | 播放器需支持透明叠加层 | 前端预留叠加层架构，后续可加 |
| Q2 | 是否需要标注鼠框（精确到个体） | 标注交互和数据模型变更 | 当前不实现，不影响现有设计 |
| Q3 | 行为类别字典来源 | ~~前端类别列表来源~~ | ✅ 已确定：按北医目录初始化 12 个项目级类别；P1 通过 API 读取，P2 提供管理界面 |
| Q4 | 视频和导出片段的存储路径 | ~~服务器硬盘分配~~ | ✅ 已确定：P1 实际默认路径为 `backend/data/videos/`（视频）与 `backend/data/exports/`（导出片段），运行时数据已 gitignore；部署时映射到 `/NVme` |

## 9. 当前实现状态与下一步

### 9.1 P1 已实现（本地原型）

- 后端（FastAPI + SQLite）与前端（React + TypeScript + Vite）P1 原型已实现：pytest 38 passed、前端 `npm run build` 通过，真实 HTTP smoke 覆盖 demo 登录、项目、类别（确认 12 类）、视频、标注、统一事件 JSON 导出与视频流。
- 已实现功能：登录、项目入口/项目内身份、创建项目时初始化北医 12 类、Mock 视频元数据、视频流、区间标注 CRUD、时间轴/快捷键、统一事件 JSON 导出。
- 演示数据由 `backend/scripts/seed_demo.py` 提供（demo/demo123 仅开发用）；启动与测试说明见 `backend/README.md`、`frontend/README.md`。

### 9.2 尚未实现（P1 边界）

- 真实文件上传、ffmpeg 片段裁剪导出、审核/任务分配、类别管理界面、YOLO/画框叠加、生产部署均未实现。
- 浏览器中的 MOV 编码兼容性与完整视觉流程仍需人工验收后确认。

### 9.3 下一步

1. 本地浏览器人工验收：demo 登录、项目入口、视频播放（MOV 兼容性）、时间轴标注与 JSON 导出的完整视觉流程。
2. 验收通过后再确定真实上传、ffmpeg 裁剪与 P2（审核、任务分配、类别管理）的排期。
