# 标注网站服务器选型速查

> 用途：填写还要上传的视频小时数、同时使用人数和全部视频的片段容量上限，即可快速选择服务器；估算基线日期为 2026-08-10。

## 可以发送给供应商agent的brief~

CPU/内存：2C4G，可升至4C8G；
存储总量：160 GiB > 140 GiB（40–60 + 100），满足总存储不低于要求，且便于扩容；
无 GPU 依赖，适用于 Web 视频服务场景；
支持 HTTP Range 和缓存（应用层实现，服务器资源足够支撑）；
峰值不低于30 Mbps
打包ipv4公网地址


## 当前建议

- **CPU / 内存**：1–5 人可从 2 vCPU / 4 GB 经济起步，无 GPU；不满足触发条件时升级至 4 vCPU / 8 GB。
- **系统盘**：40–80 GB。
- **数据盘**：按下文公式选档；只有 `H≈0` 且片段容量上限较小时，才从 100 GB 起步。
- **公网峰值**：播放优化完成后，5 人同时播放按约 25 Mbps 计算，选择 30 Mbps 档位。

## 已知数据

- 当前原视频：19.74 GB；其低码率播放副本因总时长未实测，按同一容量比例粗估约 5.5 GB。
- `H`：还要上传的原视频小时数，由使用者填写。
- `N`：同时标注或播放人数；当前总峰值不超过 5 人，同时播放/拖动峰值为 5 人。
- 每天最多 100 条行为，每条片段不超过 10 秒；片段在 24 小时内生成可接受。
- 原视频约 6.5 GB/小时，播放副本约 1.8 GB/小时；每新增 1 小时视频合计按 8.3 GB 估算。

## 按剩余视频小时数选数据盘

`最低数据盘容量(GB) ≈ (19.74 + 5.5 + 8.3 × H + C_clip_max + 10) ÷ 0.8`

- `H`：还要上传的原视频小时数。
- `C_clip_max`：全部已上传视频最多会占用的片段容量。该值应按每个视频的片段上限汇总；当前上限尚未确认，不能编造数值。
- `10 GB`：临时导出、数据库和日志预留；`0.8`：数据盘用到 80% 即扩容。
- 算完后向上选择 100 / 200 / 500 / 1000 GB 或更大档位。

下表暂取 `C_clip_max = 0`，**未计入任何片段容量**。真实选盘前必须先把片段上限填入公式再重算。

| 还要上传 H | 计算结果 | 建议数据盘 |
|---:|---:|---:|
| 0 小时 | 约 44 GB | 100 GB |
| 5 小时 | 约 96 GB | 100 GB（非常接近扩容线，通常建议 200 GB） |
| 10 小时 | 约 148 GB | 200 GB |
| 20 小时 | 约 252 GB | 500 GB |
| 50 小时 | 约 563 GB | 1 TB |
| 100 小时 | 约 1082 GB | 至少 2 TB |

片段容量由已上传视频及每个视频的标注上限决定。没有新增视频时，不会仅因月份增加而持续增长。

## 按同时标注人数选 CPU 和内存

| 同时标注人数 | 建议规格 |
|---:|---|
| 1–5 人 | 2 vCPU / 4 GB，经济起步 |
| 6–10 人 | 4 vCPU / 8 GB |
| 超过 10 人 | 先压测，并重新评估数据库和部署方式，不直接猜规格 |

即使只有 1 人也不建议低于 2C4G，因为服务器还要用 FFmpeg 生成片段和低码率播放副本；无需 GPU。

出现任一情况就升级至 4C8G：转码持续积压、片段或播放副本不能在 24 小时内完成、CPU 长期超过 80%、内存长期超过 80%、持续使用 swap 或发生 OOM、上传或编码时页面明显卡顿。

## 按同时播放人数选公网峰值

低码率播放副本按 4 Mbps 保守值并计入 1.2 倍余量后，直接按 **约 5 Mbps × 同时播放人数** 选型。

| 同时播放 N | 计算峰值 | 建议档位 |
|---:|---:|---:|
| 1 人 | 约 5 Mbps | 5–10 Mbps |
| 2 人 | 约 10 Mbps | 10 Mbps |
| 3 人 | 约 15 Mbps | 20 Mbps |
| 5 人 | 约 25 Mbps | 30 Mbps |

“按流量计费”决定如何收费，“公网峰值能力”决定同时播放是否顺畅，两者必须分别核对。

## 费用速算

| 数据盘 | 参考月费（0.35 元/GB/月） |
|---:|---:|
| 100 GB | 约 35 元 |
| 200 GB | 约 70 元 |
| 500 GB | 约 175 元 |
| 1000 GB | 约 350 元 |
| 2 TB | 约 700 元 |

总价按 `实例实时价 + 系统盘 + 数据盘 + 流量 + 快照` 估算。2C4G 尚无已核实价格，不填写固定价格；4C8G 约 220–280 元/月仅为调研的升级档参考。