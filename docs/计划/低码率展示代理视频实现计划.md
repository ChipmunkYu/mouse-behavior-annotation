# 低码率展示代理视频实现计划

> **状态（2026-08-31）：P1—P4 与导出热修已以 `50be725743254c0fa55ae3b21de646d457211417` 部署至生产，数据库 schema 为 `0016`；service active、health ok，用户公网人工确认上传、预览、标注、审核和 ZIP 导出可用。** `2751e98d180ee034dce64e9ca7b4e47722bb8c7d` 仅是加载进度前端 release 和前一生产版本。本文记录实现、生产切换与剩余扩展验收；生产实时事实与服务器文件路径只查仓库外“网站服务器文件清单”。

## 1. 已实现契约

- `0016_display_proxy.py`、展示代理字段、持久任务 ownership、单 owner worker、CAS、恢复、原子发布、磁盘保护和硬删除闭环已实现。
- 新 file-backed Video 仅在 `DISPLAY_PROXIES_ENABLED=true` 时计算原片 SHA-256，并在同一事务创建代理任务；commit 后只负责唤醒 worker。multipart 上传和三文件完成入口遵循同一规则，metadata-only Video 明确排除。
- Preview、Clips、Review、Annotate 四入口共用同一媒体能力：完整认证 GET → 流式读取并显示真实下载进度 → 完整累积 Blob → `URL.createObjectURL()`；切换或卸载会 abort 旧请求、屏蔽晚到响应并回收 object URL。响应存在 `Content-Length` 时显示确定进度，否则显示已加载字节和不确定进度，不使用伪进度。
- 开关开启时，ready 才可展示；pending/processing 返回 `409 + DISPLAY_PROXY_PENDING`，failed、缺失或不一致返回 `409 + DISPLAY_PROXY_FAILED`，不静默读取原片。
- `Video.storage_path` 始终指向原片。片段生成、Submission 冻结、缺失片段补生成和项目导出始终读取原片，展示代理不得成为业务权威。
- 候选 profile 为 1280×720、H.264/libx264、CRF 28、veryfast、yuv420p、固定 30 帧 GOP、SAR 1:1、无音频和 faststart；profile version 明确标记 `ordinal-cfr`。代理不复制源 VFR 的逐帧 PTS，而是按解码帧序号一一对应：保留源 `avg_frame_rate` 的原始 `NUM/DEN`，令代理第 `i` 帧的归一化 PTS 约为 `i / source_avg_fps`，从而生成高精度 CFR，保护业务 `frame/fps`、轨迹帧与画面帧的对应关系。发布前校验 profile、ordinal CFR 时间轴、帧数、旋转与 source SHA-256。profile 升级后，删除流程可识别并清理受支持历史版本的 terminal job，但 worker 仍只接受当前版本，历史 job 不可重跑。

### 1.1 候选与生产验证记录（2026-08-31）

- 候选运行环境为 Python 3.10.12、FFmpeg/ffprobe 4.4.2。`c6bcfb0` 完成功能与本地/服务器测试；Node 22 测试暴露的 Blob 测试可移植性问题由 `0c01101` 修复；真实服务缺少展示代理结构化事件的问题由最终提交 `ba99fc9` 修复。
- 服务器证据包括：`c6bcfb0` 后端聚焦 `119 passed, 1 skipped`、真实 display processor round-trip `1 passed`；`0c01101` 前端 `15 passed` 及 `api:check`、typecheck、build；`ba99fc9` 后端关键复核 `22 passed`、原片 authority/真实片段 `5 passed`、日志/预检 `14 passed`，且 `pip check` 通过。
- 隔离 production 形态的空数据库已迁移至 `0016`，初始 preflight PASS。单实例端到端上传 3 秒 1920×1080/30 FPS H.264 原片后，代理约 2.177 秒 ready；原片 3293974 bytes、代理 242512 bytes。strict 响应为 `display-video-1.mp4`，source/proxy authority 与 source identity 均通过；代理为 H.264、1280×720、SAR 1:1、yuv420p、30 FPS、90 帧、3 秒。该历史短样本证据早于 `ordinal-cfr` profile version，不能替代新 profile 的服务器验收。
- 完成后 preflight 为 `1/1 ready`、`active=0`；真实输出 enqueue/claim/ready/strict-ready 结构化日志，敏感模式扫描 PASS，且无 `.part`。隔离候选服务已停止。
- 生产切换历史：`e77` 上线空 `0016` strict 后，首个真实 VFR 因旧 CFR 门禁失败；`16dafb3` 放宽输入但输出出现 65μs/66.667ms 量化与 16.667ms 漂移，未部署；`083ed18` 改为 ordinal CFR 并通过真实门禁后短暂上线；`34965b1` 兼容历史 profile terminal job 删除，但 worker 仍只接受当前 profile；`2751e98` 增加四入口真实加载进度并成为前一生产版本；当前生产为导出热修 `50be725743254c0fa55ae3b21de646d457211417`。
- ordinal CFR 真实服务器门禁直接处理此前 401010242-byte、5402 帧真实 VFR 源，143.323 秒生成 4635602-byte 代理；源 SHA-256 `0b3fcff25c5c2c9190d4ba50e5a8a19b0ac59d363c686611d97937c7f009fe94` 不变，代理 SHA-256 `db00a7c5894659575ba5be90528ed26c58f7a6574dbfb09cb970e0744e6686e9`。代理为 H.264/1280×720/yuv420p/SAR 1:1，avg `27010000/899917`、time_base `1/30000`、5402 帧、duration 179.9834 秒；帧间隔 33.300–33.334ms，最大 ordinal PTS 误差约 0.017ms，完整解码通过。
- `2751e98` release 约 213M；后端 `pip check`、`compileall` 通过，前端 Node 22/npm 10 下 4 files/21 tests、API check、typecheck 与 77 modules production build 通过，新静态资源为 `index-CMqG60bM.js`、`index-D6NQgxXp.css`。`npm install` 报告 3 moderate、2 high，未执行自动修复。该版本现为前一生产，不是当前生产。
- 公网人工验收中，新视频 `WIN_20260821_14_20_49_Pro_part001.mp4` 为 Video 1，duration 185.5688333、fps 29.9673167、`display_status=ready`、profile `candidate-720p-h264-crf28-g30-sar1-ordinal-cfr`；Job 6 `succeeded`、progress 100、attempts 1，且无 `.part`。用户已确认上传、预览、标注和审核可用。
- 首次项目导出的 Job 8 因 ready clip 的 canonical FPS `29.9673167` 与 probe FPS `30` 被严格 `1e-6` 比较拒绝。`50be725` 将视频时间轴门禁调整为累计时基漂移不超过一个 canonical frame，同时保留精确 decoded 帧数/尺寸、独立 duration 一帧门禁及 tracks/ZIP 门禁；聚焦验证为 `59 tests`、`compileall`、`diff check`，服务器对同一 238 帧 clip 的合同复核通过。切换 `50be725743254c0fa55ae3b21de646d457211417` 后，service active、health ok、schema `0016`；Job 12 `succeeded`，结果为 `export_project_1_12.zip`，用户公网人工确认 ZIP 导出可用。
- 本次人工结论限于上传、预览、标注、审核和 ZIP 导出可用，不包含三浏览器、并发、ENOSPC或压力测试。
- 首次切换自动回滚备份为 `/data/mouse-annotation/backups/pre-e77-20260831T040517Z/`；成功空库切换对应的旧生产完整回滚备份为 `/data/mouse-annotation/backups/pre-e77-20260831T040937Z/`，含 `data-original`、独立 `annotation-0015.db`、`backend.env`、manifest 与 preflight 证据。旧数据不迁移。

## 2. 最终范围与唯一开关

最终方案明确：

1. **不支持历史 backfill。** 不提供 hash/代理回填 service、CLI 或启动扫描。
2. **没有独立 fallback 配置。** 不提供 `DISPLAY_PROXY_ALLOW_SOURCE_FALLBACK` 或按页面降级。
3. **只有 `DISPLAY_PROXIES_ENABLED`：**
   - `false`：四入口播放原片，新 file-backed Video 不 hash、不入队；
   - `true`：新 file-backed Video hash/入队，四入口严格只读 ready 代理。
4. 开关为 `false` 期间必须禁止新视频写入，否则这些视频日后启用 strict 时没有代理。
5. 启用前必须在维护窗口清空或重置全部旧 file-backed 数据，并留存操作证据。用户已确认可以清空，但实际服务器执行前仍须再次明确确认具体对象、备份和删除命令。

代理目录固定由 `DATA_DIR` 派生为 `DATA_DIR/display_proxies/`，不提供 `PROXIES_DIR`。配置模板固定包含：

```dotenv
DISPLAY_PROXIES_ENABLED=false
DISPLAY_PROXY_TIMEOUT_SECONDS=3600
DISPLAY_PROXY_MAX_ATTEMPTS=3
DISPLAY_PROXY_SYNCHRONOUS=false
DISPLAY_PROXY_DISK_RESERVE_BYTES=1073741824
```

生产只允许单实例、单 Uvicorn worker；禁止滚动发布、禁止新旧实例并存、禁止两个 owner 共享 SQLite 和代理目录。

## 3. 只读 preflight

生产从目标 release 的 `backend/` 运行部署指南所列安全 Python wrapper，并显式传入
`--env-file /home/jinghan/.config/mouse-annotation/backend.env`。禁止 `source backend.env`、
`. backend.env` 或 `set -a`；wrapper 先核对 `ENV=production`、生产数据/数据库目标及开关仍为
`false`，但不得打印 env 内容或 secret。本地开发才可直接运行 `python scripts/display_proxy_preflight.py`。

该命令只读数据库与已有文件状态：不写库、不计算 source hash、不运行 ffprobe，也不创建代理任务。

| 退出码 | 含义 | 处理 |
|---|---|---|
| `0` | 所有受管 file-backed Video 均满足 strict ready 条件，或库为空 | 才允许继续把开关切为 true |
| `1` | 存在 pending/failed/缺失/身份不一致等未就绪数据 | 保持停写和开关 false，处理旧数据后重跑 |
| `2` | 配置、数据库连接或检查过程错误 | 停止上线并排障 |

上线 strict 前最后一次 preflight 必须返回 `0`；旧输出或其他环境的结果不能替代。

## 4. 安全部署顺序

以下顺序已用于本次生产切换，继续作为后续发布与回滚基线；具体服务器实时结果以服务器清单为准：

1. 固定完整 commit，创建不可变 `releases/<commit>/`。
2. 安装锁定后端依赖并 `pip check`；执行前端 `npm ci`、类型/API 检查和 production build。
3. 冻结入口、所有新视频上传/三文件完成及其他新写入，等待 display、media、export、cleanup 等相关任务进入已知终态。
4. 停止唯一 systemd 服务，确认 unit inactive、Uvicorn 退出且 8000 不再监听；后续迁移/清理期间禁止后台 worker 运行。
5. 创建 SQLite 一致性备份并对备份执行 `PRAGMA integrity_check`；记录备份路径、权限和结果。
6. 显式迁移到候选 head `0016` 并核对 revision；处理旧 file-backed 数据前再次明确对象、范围、备份与不可逆影响并留证。
7. 创建由 `DATA_DIR` 派生的代理目录，核对 owner/group、`0750` 目录权限、同文件系统原子发布条件及磁盘余量。
8. 保持服务停止和停写，使用显式 `--env-file` 的安全 wrapper 运行 preflight；只有退出码 `0` 才把稳定生产 env 的 `DISPLAY_PROXIES_ENABLED` 改为 `true`。
9. 原子切换到目标 release，启动**单实例、单 Uvicorn worker**；不得滚动双实例。
10. 上传一个有代表性的真实视频，等待代理从 pending/processing 到 ready。
11. 验证 Preview、Clips、Review、Annotate 都完整下载同一代理；验证 pending/failed 409；验证片段、Submission/审核和项目导出仍来自原片。
12. 检查应用/systemd/Nginx 日志无凭据和路径泄漏，检查代理/临时文件、磁盘 reserve、CPU、内存与 I/O；证据完整后才解除维护窗口写入。

## 5. 回滚

回滚前先冻结新视频上传并等待相关任务 terminal，再停止唯一服务。紧急停机遗留 `queued`/`running` display job 时，外部入口和所有写入必须继续冻结，且不得手工改 DB。临时以同版本、`DISPLAY_PROXIES_ENABLED=true`、唯一服务、单 Uvicorn worker 启动 recovery/worker，全程不得向用户开放；逐项监测任务到已知终态，任何任务失败、无法可靠进入终态或状态未知都必须阻断并专项处理。成功收敛后再次停止服务，确认 unit inactive、Uvicorn 退出且 8000 未监听，才继续备份、迁移、preflight 和正式启动；不得启动第二实例或并行运行其他服务。

### 一级：同 release 事故读回退

将 `DISPLAY_PROXIES_ENABLED=false` 并重启同一 release，使展示临时读取原片。此举只用于事故恢复，不表示代理兼容模式；**必须持续冻结新视频写入**，否则会产生未 hash/未入队数据，阻断再次启用 strict。保留数据库 `0016` 和代理文件以便排障。

### 二级：回到旧 release

在服务停止且备份已确认的维护窗口，先处理 schema：按迁移链从 `0016` downgrade 到 `0015`，或恢复上线前一致性备份，再切换旧 release。迁移后产生的用户写入可能在 downgrade/恢复备份时丢失，执行前必须明确数据丢失窗口并再次确认。禁止只切代码让旧 release 读取未知 schema。

## 6. 剩余扩展验收

以下尚未完成，不影响本次“已部署且公网人工确认上传、预览、标注、审核和 ZIP 导出可用”的结论，但不得伪装为已通过：

- 更多秋采时长样本的帧数、首尾帧、`frame/fps` 时间轴、轨迹帧与标注叠加抽查；
- 真实 ENOSPC 下 temp 清理、失败状态、原片保护和恢复；
- 代理生成与在线请求、片段、导出的并发 CPU、RSS、I/O 和磁盘资源量化；
- Chrome、Edge、Firefox 对目标大小大 Blob 的下载、内存/临时盘、取消、切换和释放行为；不得由本次公网人工验收外推。

当前统一表述为“P4 与导出热修已部署，公网人工确认上传、预览、标注、审核和 ZIP 导出可用；真实 ENOSPC、并发资源、三浏览器目标大 Blob取消/切换及更多秋采时长与叠加抽查待扩展验收”。原片继续作为业务权威。
