# 项目术语表

## 1. 适用范围与权威性

本文是本项目界面文案、需求、设计、前后端说明和导出说明的**权威术语基线**。当前项目文档与本文冲突时，以本文为准；代码标识符、API 路由、JSON key、枚举值和文件名不因产品术语调整而改名。`参考文档/` 下的外部或历史材料不属于当前术语与工作流依据。

## 2. 规范术语表

| 产品用语 | 技术术语 / 字段 | 定义 |
|---|---|---|
| track | display track | 一组按时间关联的检测结果。修正后 track 限定在一个视频的一次检测结果导入修订内；旧 `CorrectedTrack` 模型仅作迁移兼容。 |
| track ID | `track_id`、`display_track_id` | track 的整数标识符；一个 track 有一个 track ID。它不是跨视频生物身份。 |
| 参与对象 | `mouse_ids` | 行为涉及的目标集合；技术上是修正后 track ID 的去重有序数组。`mouse_ids` 是历史兼容字段名，语义与目标种类无关。 |
| 对象数量 | `mouse_count_min` / `mouse_count_max` | 行为类别允许的参与对象数量范围。这两个字段是历史兼容名称，语义与目标种类无关。 |
| 参与对象状态 | `mouse_id_status` | 行为标注的参与对象选择状态。该字段是历史兼容名称，语义与目标种类无关。 |
| track 修正 | workspace / identity-edit workspace | 用户修正 track 归属的工作模式。 |
| Split | identity edit | 从指定帧把一个 track 分成前后两个 track 的规范操作名。 |
| Merge | identity edit | 把多个 track 归并到保留 track 的规范操作名。 |
| 忽略整个 track | detection suppression, corrected-track scope | 冻结并抑制该修订下 track 对应的检测集合。当前产品不提供单框创建或单框抑制；历史 `scope=detection` 数据仅作兼容。原始检测保持不可变，不执行物理删除。 |
| 撤销上一次 track 修正 | unified undo | 按实际完成顺序撤销当前页面会话中最近一次可追踪的 Split、Merge 或忽略整个 track；刷新后不恢复统一历史。 |
| 行为标注 | `Annotation` | 用户创建和维护的标注记录。 |
| 标注区间 | `start_time` / `end_time`、`start_frame` / `end_frame` | 行为标注在时间或帧上的范围。 |
| 行为事件 | exported JSON record | 单视频 legacy JSON API 中的导出记录；正式项目 ZIP 使用每片段 `annotation.json`。 |
| 视频片段 | `Clip`、MP4 | 按标注区间生成的 MP4 媒体产物。 |
| 片段库 | clips collection page | 跨视频浏览审核通过视频片段的集合页面。 |
| 三文件导入批次 | video import batch | 原始视频、`tracks.jsonl`、`metadata.json` 组成的导入批次；三个槽位可分别上传和重试。 |
| 检测结果导入 | `DetectionImport` | 结构化检测结果及元数据的一次导入对象。 |
| 修正后 track 结果 | `tracks.corrected.jsonl` | 按帧组织并覆盖 `0..frame_count-1` 的下游 track 产物；空帧写 `detection_count=0`、`detections=[]`，可重新导入。 |
| 项目角色 | `ProjectMembership.role` | 项目内权限身份，固定为 `owner/admin/member`；用户没有全局角色。 |
| 项目所有者 | `role=owner` | 创建项目的受保护项目成员；不能被降级或移除。界面普通文案使用“项目所有者”，精确枚举值保留 `owner`。 |
| 管理员 | `role=admin` | 可管理成员、邀请码和视频分工，并具备审核能力的项目成员。 |
| 成员 | `role=member` | 不含管理权限的项目成员；可否审核由 `can_review` 独立决定。 |
| 项目成员 | `ProjectMembership` | 用户与项目之间的成员关系。active 项目成员可编辑项目数据、领取视频和提交审核。 |
| 审核能力 | `can_review` / effective review permission | owner/admin 始终具备；member 仅在 `can_review=true` 时具备。审核能力不限制标注编辑，允许自审。 |
| 当前负责人 | `Video.assignee_membership_id` / `assignee` | 当前对完整视频承担处理责任的 active 项目成员；可空。只表示责任归属，不构成编辑权限隔离。 |
| 领取 | `POST .../claim` | 项目成员将一个未分配视频原子设为由自己负责；并发时只有一个请求成功。 |
| 释放 | `POST .../release` | 当前负责人将自己的 `draft` 视频恢复为未分配；不删除已有数据。 |
| 分配 | assignment | owner/admin 将一个或多个 `draft/rejected` 视频的当前负责人设为指定项目成员。 |
| 改派 | reassignment | owner/admin 将已有当前负责人的视频分配给另一项目成员。 |
| 清空负责人 | assignment clear | owner/admin 将视频当前负责人置空；不要写作“删除任务”。 |
| 分配状态 | assigned / unassigned | 视频是否存在当前负责人，仅包含“已分配/未分配”；不是视频工作流状态或独立任务状态。 |

## 3. 必须区分：track、track ID、参与对象

- **track** 是检测随时间形成的对象；**track ID** 只是该对象的标识符，不能把二者互换。
- 一个修正后 track 有一个 track ID。track ID 仅在一个视频的一次 `detection_import_revision` 内有效，不能解释为跨视频同一生物个体。
- **参与对象**是界面语义；`mouse_ids` 是历史兼容技术表示，数组元素是指定检测结果导入修订下的修正后 track ID。
- 普通文案需要谈参与语义时写“参与对象”，需要谈标识符时写“track ID”；对象计数写“对象数量”。

## 4. 标注与媒体生命周期对象

`行为标注`（数据库记录）通过`标注区间`描述时间范围；导出后成为 JSON 中的`行为事件`。媒体 worker 可据此生成 MP4 `视频片段`，审核通过的有效视频片段可在`片段库`浏览。以上对象不得统称为“片段”或“事件”。

单视频 `/annotations/export` 是 legacy 兼容 JSON API。正式项目 ZIP 是独立四文件片段契约，每目录恰含 `clip.mp4`、`annotation.json`、`tracks.json`、`metadata.json`，不含集中式 `annotations.json`、manifest 或 `corrected_tracks/`。

## 5. 版本与修订矩阵

面向用户统一使用“对象 + 版本”，技术文档统一使用“对象 + 修订”。禁止脱离对象单写“版本”或“修订”。

| 用户界面 | 技术名称 / 字段 | 何时变化 | 审核是否锁定 |
|---|---|---|---|
| 检测结果导入版本 | 检测结果导入修订 / `detection_import_revision` | 替换 tracks/metadata | 是 |
| track 修正版本 | track 修正修订 / `identity_revision` | `Split`、`Merge`、检测抑制或撤销 | 是 |
| 行为标注版本 | 行为标注修订 / `annotation_revision` | 标注语义变化 | 是 |
| 媒体版本 | 媒体修订 / `media_revision` | 源视频、标注区间或裁剪区域变化 | **否，单独管理** |

审核快照锁定前三类修订；`media_revision` 只决定视频片段及缩略图是否需要重编码，不属于审核锁定的三类修订。

## 6. 两套状态不得混用

- **视频工作流** `workflow_status`：草稿 `draft`、待审核 `submitted`、已通过 `approved`、已退回 `rejected`。它描述整条视频工作流。
- **行为标注审核状态** `Annotation.review_status`：`pending`、`approved`、`rejected`。它描述单条行为标注，不含 `draft` 或 `submitted`。

提交视频时，本修订内行为标注进入 `pending`；审核视频后，行为标注相应进入 `approved` 或 `rejected`。二者相关但不是同一字段或同一状态机。

Split、Merge、对应撤销、整轨检测抑制及其撤销会重校验视频内全部 Annotation 并推进有效项修订；无效项进入 `needs_mouse_ids`。视频仅在当前为 `submitted/approved` 时退回 `draft`；单条标注仅在实际受影响且原为 `approved` 时改为 `pending`，不能写成“全部审核状态重置”。视频库筛选使用 `workflow_status`。

## 7. 兼容标识符与规范产品英文

- 产品文案和规范正文优先使用 literal lowercase `track`、`track ID`、`Split`、`Merge`；不得把这些产品词翻译为中文。
- `mouse_ids`、`mouse_id_status`、`mouse_count_min/max`、`identity_revision`、identity-edit API/模型名称均为历史兼容标识符，保持不变；它们的产品语义是目标中立的参与对象或 track 修正语义。
- 代码、API、JSON、数据库、枚举值、路径、文件名和命令中的精确标识符保持不变。
- 普通文案统一使用“当前负责人”；仅代码/API 字段使用 `assignee` 或 `assignee_membership_id`。项目角色普通文案使用“项目所有者 / 管理员 / 成员”，精确枚举值保留 `owner/admin/member`。

## 8. 弃用或歧义用语

| 不再用于当前项目的说法 | 替换为 |
|---|---|
| 中文化的 track 产品词 | track |
| 中文化的 track ID 产品词 | track ID |
| 面向用户的身份修正、身份校正 | track 修正 |
| 中文化的 Split 操作标签 | Split |
| 中文化的 Merge 操作标签 | Merge |
| 删除整个 ID、删除整个 track | 忽略整个 track |
| 三文件上传 | 三文件导入批次 |
| YOLO 导入 | 检测结果导入 |
| corrected tracks（面向用户） | 修正后 track 结果 |
| 标注者/审核者（指项目角色） | 成员 + 审核能力（分别说明） |
| reviewer / annotator 项目角色 | `member` + 对应 `can_review` |
| 负责人、处理人、任务负责人混用 | 当前负责人 |
| 取消领取、退领 | 释放 |
| 取消分配、删除任务 | 清空负责人 |
| 任务状态（指分工） | 分配状态或视频工作流状态（按实际语义） |
| 标注片段（指记录） | 行为标注 / 标注区间 |
| Clip（普通文案） | 视频片段 |
| 无对象前缀的“版本/修订” | 检测结果导入、track 修正、行为标注或媒体 + 版本/修订 |

检测抑制只影响渲染、校验、统计和修正后 track 结果；原始文件与 `RawDetection` 始终不可变。任何文档都不得把忽略操作描述为物理删除。

## 9. 写作规则

1. `track` 必须保持 literal lowercase；`track ID` 必须使用 ASCII 空格且 ID 大写。
2. 操作标签统一使用 `Split` 和 `Merge`，不使用中文化操作名。
3. 泛化产品语义统一使用“参与对象”“对象数量”“对象列表”或“track ID 列表”。
4. 不改写代码块、API 路由、JSON key、枚举值、文件名和可执行命令中的技术标识符。
5. 统一撤销只能写作“撤销上一次 track 修正”；其范围按实际完成顺序覆盖当前页面会话中的 Split、Merge 和忽略整个 track，并须明确刷新后不恢复统一历史。
6. 说明导出时区分页面和 worker：导出页面只创建并监控后台任务，不同步调用 ffmpeg；项目导出 worker 可以在打包前补生成缺失的已通过视频片段。
7. ZIP 路径规范写作 `clips/<group>/<category>/...`；ZIP 行为事件必须有 `clip_file`。
8. 分工正文统一使用“当前负责人、领取、释放、分配、改派、清空负责人、分配状态”；不得创造独立“任务”对象或把分配状态与视频工作流状态混写。

## 10. 示例

- 推荐：选择参与对象后，`mouse_ids` 保存为当前检测结果导入修订下的修正后 track ID 数组。
- 推荐：在 track 修正模式中执行 `Split`，会产生新的 track 修正修订。
- 推荐：确认整个 track 均为误检后选择“忽略整个 track”；系统冻结该 track 当前未抑制 detection 集合，原始检测保持不可变。
- 推荐：项目导出 worker 为每个 `SubmissionAnnotation` 生成独立目录，并写入固定的 `clip.mp4`、`annotation.json`、`tracks.json`、`metadata.json`。
