# 前端键盘快捷键优化方案

> 状态：已修复数字类别映射与参与对象键盘导航；代码级验证通过，仍需人工浏览器验收
> 范围：进入视频行为标注页后的核心操作
> 目标：用少量、稳定的快捷键加速高频行为标注流程，同时保留按钮和鼠标操作；不再追求所有功能都能仅靠键盘完成。
> 术语基线：`项目术语表.md`。本文同时记录当前实现、类别映射规则及刷新边界。

## 1. 当前实现与本轮计划

### 1.1 当前已经实现

以下结论以当前前端实际按键处理逻辑为准：

| 按键 | 当前操作 | 生效范围 |
|---|---|---|
| `Space` | 播放/暂停 | 全局；输入框、按钮或确认弹窗聚焦时不触发页面播放 |
| `←` | 后退 1 帧 | 全局；输入框或确认弹窗聚焦时不触发 |
| `→` | 前进 1 帧 | 全局；输入框或确认弹窗聚焦时不触发 |
| `Shift+←` / `Shift+→` | 后退/前进 10 帧 | 全局；输入控件、弹窗或局部时间轴聚焦时不触发；忽略长按重复 |
| `S` | 设置行为标注起点 | 仅行为标注模式 |
| `D` | 设置终点并创建行为标注 | 仅行为标注模式，且前置条件满足时 |
| `1`–`9`、`0` | 按稳定显示顺序选择前 10 个启用行为类别 | 仅行为标注模式、视频就绪且不在输入/弹窗/编辑状态 |
| `↑` / `↓` / `Space` | 移动参与对象高亮并多选 | 仅参与对象导航状态 |
| `Delete` | 打开当前模式目标的危险确认框 | 必须存在明确目标；不直接执行 |
| `Ctrl+Z` | 撤销当前页面会话中最近一次可追踪的 track 修正 | 仅 track 修正模式；刷新边界见 4.4 |
| `?` | 打开/关闭快捷键帮助 | 输入控件中不触发 |
| `Esc` | 取消确认、关闭帮助、取消编辑或退出参与对象导航 | 一次只关闭一个明确状态 |

### 1.2 当前实现边界

- 类别模型没有显式的“群体行为快捷键”或“快捷键已启用”字段。前端不使用名称常量、`group`分组或参与对象数量猜测是否排除类别。
- 类别接口只返回启用类别，并按 `sort_order`、`id`稳定排序；前端再次按相同字段稳定排序，取前 10 个启用类别映射 `1`–`9`、`0`，第 11 个以后不映射。
- `Ctrl+Z`只维护当前页面会话内成功返回可撤销 ID 的操作，按实际完成顺序撤销。刷新后不会伪造无法恢复的 `Split` / `Merge`历史。
- 已取消键位没有注册；未列入最终集合的操作继续使用按钮或原生控件。

### 1.3 **下一轮快捷键调整计划（待实现）**

> **本节全部为已确认的下一轮目标，不表示当前代码已经实现。** 第 1.1 节以及后文标为“当前”“已实现”的内容仍是现状记录；完成代码修改与浏览器验收后，才能更新实现状态。

#### 键位与调度规则

1. **裸 `Tab`切换模式**：在“行为标注”和“track 修正”两种模式间切换；`Tab`不再承担页面原生焦点遍历，也不再用于 track 列表下移。进入 track 修正的前置条件不满足时（例如没有有效 detection import），保持行为标注模式并提示具体原因。
2. **`Shift+Tab`尚未确认**：不得自行赋予新业务功能；实现前需决定是消费该按键还是令其与裸 `Tab`同义，当前不作结论。
3. **`T`显式进入/退出当前模式的 track 列表键盘导航**：用户已确认采用该方案。进入导航后，`↑` / `↓`移动高亮，`Enter`选择或取消当前高亮项；再次按 `T`退出当前模式的 track 列表导航。`Shift+↑` / `Shift+↓`当前没有独立键位定义，但现有参与对象导航未区分 `Shift`修饰键，因此会被当作普通 `↑` / `↓`消费；下一轮不为它们分配独立业务功能，并应由测试确认修饰键不会产生额外语义。
4. **`Space`只播放/暂停视频**：移除其参与对象选择和“聚焦按钮后原生激活”作为业务快捷键的职责。输入控件、局部控件与弹窗保护继续优先；确认弹窗打开时，`Space`既不播放视频，也不确认操作。
5. **选择与确认迁移到 `Enter`**：`Enter`至少负责在 track 列表导航中选择/取消当前高亮项；确认弹窗中，`Enter`执行弹窗已明确标示的确认动作，`Esc`取消。该规则是对当前“危险操作只能鼠标确认”原则的**待实现调整**，不能在实现前写成现状。
6. 输入控件、可编辑元素、快捷键帮助和确认弹窗继续构成保护边界；一次按键只能被一个最高优先级上下文消费。

#### 调整后的目标流程

```text
S → 数字键选择行为类别 → T → ↑/↓ + Enter 选择参与对象 → D
Tab：在行为标注 / track 修正间切换
T：进入 / 退出当前模式的 track 列表键盘导航
Space：仅播放 / 暂停视频
```

#### 实施检查清单与验收项

- [ ] 裸 `Tab`被页面快捷键调度受控消费，不再触发原生焦点遍历，也不再作为 track 列表下移；`Shift+Tab`策略在编码前确认并记录。
- [ ] track 修正不可用时，按 `Tab`仍停留在行为标注模式，并显示可诊断原因；覆盖无 detection import、导入无效和 track 列表不可用边界。
- [ ] `T`在行为标注和 track 修正两种模式中都只控制当前模式的 track 列表导航；`↑` / `↓`仅在导航激活时移动高亮，`Enter`选择/取消高亮项。
- [ ] 不注册 `Shift+↑` / `Shift+↓`业务快捷键。
- [ ] 页面级 `Space`只控制视频播放/暂停；对获得焦点的原生 `button`需在快捷键调度中正确 `preventDefault`，避免浏览器默认 Space 激活按钮同时产生业务动作。
- [ ] 输入控件、可编辑元素、局部时间轴、快捷键帮助和弹窗的保护顺序有明确测试；弹窗打开时 `Space`不播放、不确认。
- [ ] 确认弹窗中 `Enter`只执行当前明确的确认动作，`Esc`只取消；危险操作的文案、焦点和防重复提交边界同步验收。
- [ ] 模式切换、进入/退出导航时，哪些高亮、已选 track、行为标注起点、类别和搜索状态需要清理或保留，**实现前待确认**，不得自行猜测。
- [ ] 明确处理 `KeyboardEvent.repeat`：模式切换、选择、确认等离散动作忽略长按重复；方向键是否允许连续移动及其节流策略在实现时验证。
- [ ] 快捷键帮助、按钮文案、持续状态提示与实际处理逻辑同步，未实现键位不提前展示为可用。
- [ ] 审计 `AnnotatePage`、`ReviewPage`、`ClipsPage`的 `Space`边界：只保留各播放器的播放/暂停语义，确认不会因按钮原生激活、弹窗或输入焦点产生第二动作。
- [ ] 回归数字类别选择、`S` / `D`、`Delete`、`Ctrl+Z`、`Esc`及视频方向键，确保新调度没有恢复已取消键位或破坏当前能力。

## 2. 核心行为标注流程

当前实现的核心键盘流程是：

```text
S → 数字键选择行为类别 → ↑/↓ + Space 选择参与对象 → D
```

具体规则：

1. `S` 设置当前帧为行为标注起点。
2. 数字键选择对应行为类别。
3. 如果该类别需要参与对象、存在有效检测结果导入且参与对象列表非空，则自动进入参与对象列表键盘导航状态，并高亮第一项。
4. `↑` / `↓` 移动高亮，`Space` 选中或取消当前高亮的 `track ID`；移动后可以继续多选。
5. `D` 设置终点并创建行为标注，仍执行类别、起点和参与对象数量校验。
6. `Esc` 只退出参与对象导航状态，已经选中的参与对象保持不变。

鼠标点击类别按钮与键盘数字选择复用同一个类别选择函数；满足相同条件时都会自动进入参与对象导航，并清除旧按钮焦点，避免后续 `Space`被类别按钮或其他旧焦点吞掉。

## 3. 设计原则

1. **高频优先**：快捷键只覆盖视频定位、行为标注、参与对象选择及少量通用操作。
2. **模式清晰**：快捷键必须根据当前工作模式、焦点和导航状态产生可预测的唯一结果。
3. **输入保护**：焦点位于 `input`、`textarea`、`select` 或可编辑元素时，不触发普通页面快捷键。
4. **弹窗优先**：确认弹窗打开时暂停页面快捷键，只允许 `Esc` 取消；危险操作不提供键盘确认。
5. **危险操作不直达**：`Delete` 只打开确认框，最终确认必须点击确认按钮，不能用 `Enter`、`Space` 或再次按 `Delete` 完成。
6. **状态可见**：参与对象导航必须同时提供明显的视觉高亮和文字状态提示，高亮与已选中状态必须可区分。
7. **按键长按保护**：除明确允许连续逐帧或跳帧外，忽略 `KeyboardEvent.repeat`。
8. **提示统一**：存在对应按钮的快捷键直接写入按钮文案，统一使用英文方括号 `[]`。
9. **边界如实可见**：快捷键帮助和 track 修正面板明确说明 `Ctrl+Z`只覆盖当前页面会话。

## 4. 快捷键总表

### 4.1 全局与视频控制

| 按键 | 操作 | 状态 |
|---|---|---|
| `Space` | 播放/暂停；参与对象导航时改为切换当前高亮项 | 已实现 |
| `←` / `→` | 后退/前进 1 帧 | 当前已实现 |
| `Shift+←` / `Shift+→` | 后退/前进 10 帧 | 已实现 |
| `?` | 打开或关闭当前模式的快捷键帮助 | 已实现 |
| `Esc` | 按上下文执行取消或退出，见第 6 节 | 已实现 |

### 4.2 行为类别选择

| 按键 | 操作 | 状态 |
|---|---|---|
| `1`–`9` | 选择快捷键顺序中的第 1–9 个行为类别 | 已实现 |
| `0` | 选择快捷键顺序中的第 10 个行为类别 | 已实现 |

映射规则：

1. 当前类别模型没有可用于快捷键排除的显式群体行为字段；前端不按类别名称、`group`或对象数量猜测。
2. 类别接口只返回 `is_active`类别，并按 `sort_order`、`id`排序；前端按相同字段再次稳定排序后，将前 10 个启用行为类别映射到 `1`–`9`、`0`。
3. 单对象、双对象、群体或其他类别都按显示位置参与映射；参与对象数量只用于选择校验，不用于数字映射。
4. 第 11 个及以后的启用类别不提供快捷键，继续通过按钮选择。
5. 不设计 `Ctrl+数字`、分组前缀或任何两段数字键。
6. 有数字映射的类别按钮显示 `[1]`、`[2]`等提示；没有映射的按钮不显示虚假键位。
7. 类别顺序在同一配置版本中必须稳定，避免数字含义随刷新或筛选变化。

### 4.3 行为标注与参与对象

| 按键 | 操作 | 前置条件 | 状态 |
|---|---|---|---|
| `S` | 设置当前帧为行为标注起点 | 视频已就绪 | 当前已实现 |
| `D` | 设置终点并创建行为标注 | 已选类别、已设起点、参与对象数量符合规则 | 当前已实现 |
| `↑` / `↓` | 移动参与对象列表高亮 | 已进入参与对象导航且列表非空 | 已实现 |
| `Space` | 选中或取消当前高亮的 `track ID` | 已进入参与对象导航且存在高亮项 | 已实现 |
| `Esc` | 退出参与对象导航，保留已选参与对象 | 已进入参与对象导航 | 已实现 |
| `Delete` | 为当前已选中的行为标注打开删除确认框 | 存在通过点击明确选中的可编辑行为标注 | 已实现 |

不设置 `M` 或其他“进入参与对象导航”快捷键。数字键选中类别后按规则自动进入；通过按钮选择类别时可采用同样行为。

### 4.4 track 修正模式

track 修正保留按钮和原生交互，不再为列表导航、`Split`、`Merge`、筛选或搜索分配专属快捷键。

| 按键 | 操作 | 前置条件 | 状态 |
|---|---|---|---|
| `Delete` | 为当前选中的整个 track 打开忽略确认框 | 恰好选中 1 个 `track ID` | 已实现 |
| `Ctrl+Z` | 撤销最近一次 `Split`、`Merge` 或忽略整个 track | 当前页面会话已记录可靠的可撤销 ID | **已实现，限当前页面会话** |
| `Esc` | 关闭确认弹窗 | 确认弹窗打开 | 当前已实现 |

`Delete`不会直接忽略数据；确认框打开后必须明确操作确认按钮。`Ctrl+Z`按当前页面会话中的实际成功顺序统一撤销；服务端未返回可撤销 ID 时不入历史。刷新后既有忽略记录仍可通过记录旁的撤销按钮处理，但无法与刷新前的 `Split` / `Merge`可靠排序，因此不进入统一快捷键历史。

### 4.5 明确取消的旧键位

以下键位不再进入计划：

- `Ctrl+←` / `Ctrl+→`跳转起点、终点或视频首尾；
- 反引号切换行为标注与 track 修正模式；
- `Ctrl+B`、`Ctrl+I`、`Ctrl+K`、`Ctrl+Shift+L`控制叠加层；
- `Ctrl+数字`加第二个数字的两段类别选择；
- `M`进入参与对象导航或执行 `Merge`；
- `Ctrl+↑` / `Ctrl+↓`导航行为标注列表；
- `E`编辑行为标注；
- `Ctrl+Enter`保存编辑表单；
- track 列表的全局 `↑` / `↓` / `Enter` / `Space`导航；
- `S`执行 `Split`；
- `A`切换 track 范围；
- `/`聚焦 track 搜索；
- `Enter`确认危险操作。

## 5. 自动进入参与对象导航

数字键成功选中行为类别后，按以下顺序判断：

1. **类别不允许参与对象（`mouse_count_max === 0`）**：不进入导航，提示“该类别无需选择参与对象”。
2. **没有有效检测结果导入**：类别选择仍成功，不进入导航，并提示“没有可用检测结果，无法选择参与对象”。
3. **参与对象列表为空**：类别选择仍成功，不进入导航，并提示“当前没有可选择的参与对象”。
4. **条件满足**：进入导航，高亮当前列表第一项，并提示“参与对象选择中：↑/↓ 移动，Space 选择，Esc 退出”。

“有效检测结果导入”以 `detection-imports/current`实际返回的当前 active 导入为准，并同时要求参与对象 track 列表非空；不再使用 `revision`是否大于 0 作为二次有效性判断。

导航中的视觉要求：

- 高亮项必须有独立的焦点边框或背景，不得只依赖颜色轻微变化；
- “键盘高亮”和“已经选中”使用不同样式，可以同时存在；
- 高亮移动到滚动区域外时自动滚入视野；
- 列表刷新、筛选或当前帧变化后，高亮索引修正到有效项；
- 退出导航后移除键盘高亮，但保留所有已选 `track ID`；
- 页面状态区持续显示当前是否处于参与对象导航状态。

## 6. 焦点与按键优先级

一次按键只能触发一个动作，按以下顺序处理：

1. 确认弹窗；
2. 输入框、文本框、下拉框和可编辑元素；
3. 快捷键帮助；
4. 参与对象导航状态；
5. 当前工作模式中的业务操作；
6. 全局视频控制。

### 6.1 `Space`优先级

1. 输入控件内保留原生输入行为。
2. 参与对象导航未激活时，按钮聚焦保留原生按钮激活行为；导航激活后清除旧按钮焦点并由列表导航优先消费 `Space`。
3. 确认弹窗打开时不得触发页面播放，也不得作为危险确认快捷键。
4. 参与对象导航状态中，选中或取消当前高亮的 `track ID`。
5. 其他情况下播放/暂停视频。

退出参与对象导航后，`Space`立即恢复播放/暂停，不保留隐藏的列表状态。
参与对象导航处理器只消费 `↑`、`↓`、`Space`和`Esc`；`D`必须继续交给行为标注处理器创建行为标注。导航状态中不响应 `Delete`，避免对象选择过程中误打开其他目标的危险操作。

### 6.2 `Esc`优先级

1. 关闭确认弹窗或快捷键帮助。
2. 退出参与对象导航并保留已选参与对象。
3. 其他没有明确取消目标的场景不执行操作。

`Esc`不再承担清空搜索、取消全部 track 选择、清除行为起点等多层隐式操作。

### 6.3 方向键优先级

1. 输入控件保留原生行为；时间轴获得焦点时保留其局部 `←` / `→`行为。
2. 参与对象导航状态中，`↑` / `↓`移动高亮。
3. 其他情况下，`←` / `→`逐帧，`Shift+←` / `Shift+→`跳转 10 帧。

## 7. 按钮提示规范

1. 快捷键存在对应按钮时，直接追加到按钮文案末尾，并统一使用英文方括号 `[]`。
2. 当前目标文案包括：
   - `播放/暂停 [Space]`；
   - `后退 1 帧 [←]`、`前进 1 帧 [→]`；
   - `设起点 [S]`、`设终点 [D]`；
   - 映射了数字的类别按钮显示 `[1]`至`[0]`；
   - 参与对象导航状态提供`退出 [Esc]`按钮；
   - 当前选中行为标注的删除按钮及忽略整个 track 按钮显示 `[Delete]`；
   - 当前页面会话统一撤销按钮显示 `[Ctrl+Z]`，并在旁边说明刷新边界。
3. 第 11 个及以后的类别不显示数字提示。
4. 禁用按钮仍保留已经真实实现的快捷键提示。
5. 一个按钮支持多个真实快捷键时，在同一组方括号内用 `/`分隔。
6. 不得只在 tooltip 或帮助面板中提供按钮快捷键提示。
7. 没有处理逻辑的按钮不得显示快捷键提示。

## 8. 冲突与风险处理

| 风险 | 处理规则 |
|---|---|
| 数字键与数字输入冲突 | 输入控件聚焦时不选择类别 |
| 类别按钮顺序变化导致键位变化 | 只按固定、稳定的界面顺序映射；配置变化应视为新的类别配置版本 |
| 缺少显式群体行为快捷键字段 | 不猜测类别语义，严格映射显示顺序中的前 10 个启用类别 |
| `Space`同时控制播放和参与对象选择 | 参与对象导航状态优先，并提供持续文字提示 |
| `↑/↓`与页面滚动或局部控件冲突 | 只有参与对象导航状态消费按键；输入控件仍优先，时间轴旧焦点不再吞掉参与对象的 `↑/↓` |
| `Shift+←/→`与文本选择冲突 | 输入控件聚焦时不触发视频跳转 |
| `Shift+←/→`被普通逐帧逻辑提前消费 | 先判断带 `Shift`的组合键，再处理裸 `←/→` |
| `Delete`与文本删除冲突 | 输入控件聚焦时保留原生删除；页面操作只打开确认框 |
| `Ctrl+Z`与文本撤销冲突 | 输入控件聚焦时保留原生撤销；仅 track 修正模式消费页面快捷键 |
| `?`需要部分键盘使用 `Shift`输入 | 按实际字符 `?`判断，并提供可点击帮助入口 |
| `D`在对象未选完时误提交 | 创建前继续执行参与对象数量校验并给出原因 |
| 危险操作误确认 | 不注册 `Enter`确认；必须点击确认按钮 |

## 9. 边界条件

- 视频未加载完成时，播放、定位和创建行为标注相关键位不执行。
- 数字对应类别不存在或未映射时，不执行类别选择并给出简短提示。
- 数字类别选择成功与进入参与对象导航是两个结果：即使没有检测导入或列表为空，类别选择仍然保留。
- 类别不要求参与对象时，不进入参与对象导航。
- 参与对象列表支持继续移动高亮并多选，不因一次 `Space`选择而自动退出。
- `Esc`退出导航后不清空已选参与对象。
- 类别要求的参与对象数量不满足时，`D`不创建行为标注。
- `Delete`只有在当前模式存在明确、唯一且允许操作的目标时才打开确认框；否则仅提示原因。
- `Split`、`Merge`和忽略整个 track 正在请求时，不允许重复提交。
- 底层仍复用 track ID 修正与检测抑制两类撤销 API；前端用当前页面会话历史按实际成功顺序统一调度 `Ctrl+Z`。
- 服务端未返回可靠操作 ID 时，该操作不进入统一历史并给出提示。
- 刷新后统一历史清空；既有忽略记录可单独撤销，但刷新前的 `Split` / `Merge`无法恢复并排序，界面必须持续显示这一边界。

## 10. 前端实现方案

### 10.1 快捷键调度

继续使用行为标注页单一的 `window.keydown`监听器和最新闭包 `ref`，避免多个组件重复注册全局快捷键。

建议按优先级拆分处理器：

```text
handleModalShortcut
handleFocusedControlShortcut
handleShortcutHelp
handleParticipantNavigation
handleBehaviorShortcut
handleTrackShortcut
handleGlobalVideoShortcut
```

每个处理器返回是否消费按键；首个消费后立即停止分发。

### 10.2 已新增状态

```text
participantNavigationActive
participantFocusIndex
shortcutHelpOpen
```

数字映射应由类别配置和固定界面顺序计算，不新增两段数字键前缀状态。

### 10.3 组件职责

| 文件/组件 | 当前职责 |
|---|---|
| `AnnotatePage.tsx` | 快捷键调度、类别数字映射、参与对象导航状态、动作校验与提示 |
| `CategoryPanel` | 按稳定显示顺序展示类别，仅为实际映射的前 10 个启用行为类别显示数字角标 |
| `MouseIdsPanel` | 高亮索引、滚动跟随、多选、导航态文字和高亮/选中区分 |
| `AnnotationList` | 暴露当前明确选中的行为标注供 `Delete`打开确认框 |
| `IdentityPanel` | 保留按钮操作；为明确选中的 track 提供 `Delete`确认入口 |
| 快捷键帮助组件 | 展示当前已实现键位，并用 `?`或`Esc`关闭 |

帮助面板只展示已经实现的快捷键，并明确类别映射和 `Ctrl+Z`刷新边界。

## 11. 实施结果

### 已完成：视频定位

- 保留并回归测试 `Space`、`←`、`→`；
- 已实现 `Shift+←`、`Shift+→`跳转 10 帧；
- 完善输入焦点、按钮焦点和弹窗保护。

### 已完成：类别数字映射

- 删除名称、分组和对象数量排除规则，严格按显示顺序映射前 10 个启用类别；
- 按固定顺序映射 `1`–`9`、`0`；
- 类别按钮显示真实数字提示；
- 默认种子类别实际映射为 `1`奔跑、`2`行走、`3`静止、`4`一起、`5`接近、`6`追逐、`7`回避、`8`攻击行为、`9`鼻头接触、`0`鼻尾接触；“扎堆行为”“孤立行为”因位于第 11、12 位而不映射，不是按名称或分组排除。

### 已完成：参与对象键盘选择

- 数字选择类别后按条件自动进入导航；
- 鼠标点击类别与数字键调用同一个选择函数；当前 active 导入存在、track 列表非空且类别上限大于 0 时高亮第一项；
- 实现 `↑` / `↓`高亮移动和 `Space`多选；
- 实现 `Esc`退出且保留选择；
- 增加明显视觉高亮、滚动跟随和文字状态；
- 点击类别按钮后也进入相同导航状态，并让按钮失焦以避免 `Space`误触。

### 已完成：辅助与危险操作

- 实现 `?`帮助入口；
- 实现 `Delete`只打开确认框；
- 确保确认只能通过点击按钮完成；
- 已实现当前页面会话内的统一 `Ctrl+Z`调度，并明确保留刷新边界。

## 12. 测试与验收矩阵

### 12.1 类别映射

- `1`–`9`、`0`按固定顺序选择正确类别。
- 已启用的双对象社交行为参与映射，参与对象数量不影响数字键资格。
- 排在前 10 位的任意启用类别都响应数字键并显示提示，包括双对象或群体类别。
- 第 11 个及以后类别不响应数字快捷键。
- 输入框聚焦时数字键正常输入，不切换类别。
- 类别配置或列表刷新后映射仍确定且可解释。

### 12.2 参与对象导航

- 数字选类别后，条件满足时自动高亮第一项并显示导航提示。
- `↑` / `↓`移动高亮，越界时按确定规则停留或循环，不出现无效索引。
- `Space`支持选中、取消和继续多选，不触发视频播放。
- 导航状态中的 `D`仍创建行为标注，`Delete`不执行危险操作。
- `Esc`退出导航，保留已选参与对象；随后 `Space`恢复播放/暂停。
- 列表滚动、高亮和选中状态视觉上可区分。
- 无检测导入、列表为空或类别不要求参与对象时，类别仍选中，但不进入导航并显示正确原因。

### 12.3 焦点与安全

- 输入控件内不触发类别选择、视频跳转、`Delete`或页面 `Ctrl+Z`。
- 确认弹窗打开后，页面视频和导航快捷键全部暂停。
- `Delete`只打开确认框；`Enter`、`Space`和再次按 `Delete`均不能确认危险操作。
- `Esc`按第 6 节优先级一次只执行一个动作。
- `?`帮助打开时不会同时触发其他页面操作。

### 12.4 当前与计划边界

- 未实现的键位不出现在产品按钮和帮助面板中。
- `Ctrl+Z`只撤销当前页面会话中具有可靠操作 ID 的记录，刷新边界在帮助和面板中可见。
- 已取消键位均无处理逻辑、按钮提示或帮助条目。
- 前端构建通过，并在 Windows 主流浏览器完成按键冲突人工验收。

## 13. 验收标准

1. 用户可以按 `S → 数字键 → ↑/↓ + Space → D`完成需要参与对象的行为标注。
2. 不需要参与对象的类别可以按 `S → 数字键 → D`完成标注。
3. 数字键覆盖按 `sort_order`、`id`稳定排序后的前 10 个启用行为类别，不按名称、分组或对象数量排除。
4. 双对象、群体或其他类别本身不影响映射；第 11 个及以后类别继续通过按钮选择。
5. 参与对象导航状态有明确视觉高亮和持续文字提示，高亮与已选状态可区分。
6. 参与对象导航中的 `Space`切换选择；退出导航后恢复播放/暂停。
7. `Esc`退出参与对象导航时保留已选参与对象。
8. `Delete`不直接执行危险操作，且危险确认必须点击按钮。
9. `Ctrl+Z`只作为当前页面会话能力出现，不声称可恢复刷新前的 `Split` / `Merge`历史。
10. 所有已实现且有对应按钮的快捷键使用英文方括号显示，如 `[S]`、`[1]`。
11. 输入控件、按钮、局部时间轴和确认弹窗不与页面快捷键冲突。
12. 已取消键位没有被意外恢复。

## 14. 待确认事项

- 是否在后续接口中增加明确的“快捷键启用”或类别语义字段，以支持不依赖前 10 个显示顺序的管理员配置；当前实现不猜测；
- 是否补充可恢复、可统一排序的服务端 track 修正历史，使 `Ctrl+Z`未来能够跨刷新工作；
- 人工浏览器验收后，是否需要调整快捷键帮助面板在窄屏下的最大高度或滚动行为。

## 15. 实施验证

- `buildCategoryShortcuts`已抽成可复用纯函数，并在开发环境对真实 API 类别数组执行静态一致性断言，在控制台输出实际 `1`–`0`映射。
- 已用默认 12 类种子定义直接执行同一纯函数，得到 `1=奔跑, 2=行走, 3=静止, 4=一起, 5=接近, 6=追逐, 7=回避, 8=攻击行为, 9=鼻头接触, 0=鼻尾接触`。
- `npm run build`与`git diff --check`已通过；静态搜索确认前端不再以导入修订号判断当前 active 导入，也不再包含类别名称排除常量或旧的`mouse_count_max <= 1`过滤。
- 静态搜索确认未注册已取消的模式切换、显隐控制、搜索、编辑/保存、`Split`或`Merge`快捷键。
- 按钮快捷键提示已统一为英文方括号；无快捷键的 `Split`、`Merge`、搜索和显隐按钮不显示提示。
- 仍需在 Windows 主流浏览器人工验证焦点优先级、参与对象滚动跟随、危险确认和窄屏帮助面板。
