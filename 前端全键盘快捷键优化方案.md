# 前端全键盘快捷键优化方案

> 状态：讨论稿，待确认键位后实施
> 范围：进入视频标注页后的全部操作
> 目标：标注人员无需鼠标即可完成播放定位、行为标注、track 修正和显示控制；不同模式、输入框和弹窗之间不发生快捷键冲突。
> 术语基线：`项目术语表.md`。本文是计划文档；未明确标为当前状态的快捷键均不代表现有实现。

## 1. 当前状态

标注页已经支持以下快捷键：

| 按键 | 当前操作 | 生效范围 |
|---|---|---|
| `Space` | 播放/暂停 | 全局；输入框、按钮或弹窗聚焦时不触发 |
| `←` | 后退 1 帧 | 全局；输入框或弹窗聚焦时不触发 |
| `→` | 前进 1 帧 | 全局；输入框或弹窗聚焦时不触发 |
| `S` | 设置行为片段起点 | 仅行为标注模式 |
| `D` | 设置终点并创建行为标注 | 仅行为标注模式 |
| `Esc` | 取消确认对话框 | 仅确认对话框打开时 |

当前尚未覆盖：

- 类别选择；
- 参与对象选择；
- 标注列表导航、编辑和删除；
- track 修正模式全部操作；
- 工作模式切换；
- 大步长视频定位；
- 检测框、ID、关键点和骨架显隐控制；
- 快捷键帮助入口。

## 2. 设计原则

1. **模式隔离**：行为标注和 track 修正互斥，同一按键可在两个模式中承担不同操作。
2. **输入保护**：焦点位于 `input`、`textarea`、`select` 或可编辑元素时，普通全局快捷键不生效。
3. **弹窗优先**：确认弹窗打开时，只响应弹窗自己的确认、取消快捷键。
4. **危险操作确认**：删除行为标注、忽略整个 track 等操作仍需二次确认。整轨忽略是检测抑制，原始检测保持不可变，不执行物理删除。
5. **修饰键分层**：裸键用于高频操作，`Shift` 用于同类增强操作，`Ctrl` 用于撤销、保存和显示控制。
6. **状态可见**：快捷键触发后必须有可见反馈，包括选中、高亮、提示或错误原因。
7. **无效操作不误触**：前置条件不满足时不执行操作，并给出简短提示。
8. **按键长按保护**：除连续逐帧等明确允许的操作外，忽略 `KeyboardEvent.repeat`。

## 3. 快捷键总表

### 3.1 全局快捷键

以下快捷键在行为标注和 track 修正模式均可使用，但输入框或弹窗聚焦时不触发。

| 按键 | 操作 | 备注 |
|---|---|---|
| `Space` | 播放/暂停 | 保留现有行为 |
| `←` | 后退 1 帧 | 保留现有行为 |
| `→` | 前进 1 帧 | 保留现有行为 |
| `Shift+←` | 后退 10 帧 | 大步长定位 |
| `Shift+→` | 前进 10 帧 | 大步长定位 |
| `Ctrl+←` | 跳至已设起点；没有起点时跳至视频开头 | 行为标注模式下优先使用已设起点 |
| `Ctrl+→` | 跳至已设终点；没有终点时跳至视频结尾 | — |
| `` ` `` | 切换“行为标注”与“track 修正”模式 | 没有检测结果导入时不得进入 track 修正模式 |
| `Ctrl+B` | 显示/隐藏检测框 | Boxes |
| `Ctrl+I` | 显示/隐藏 track ID | IDs |
| `Ctrl+K` | 显示/隐藏关键点 | Keypoints |
| `Ctrl+Shift+L` | 显示/隐藏骨架 | Skeleton |
| `?` | 打开/关闭当前模式快捷键帮助 | 帮助面板打开后，`Esc` 也可关闭 |

### 3.2 行为标注模式

| 按键 | 操作 | 前置条件 |
|---|---|---|
| `S` | 设置当前帧为起点 | 视频已就绪 |
| `D` | 设置当前帧为终点并创建标注 | 已选择类别、已设置起点、对象数量符合类别规则 |
| `Esc` | 按优先级取消编辑、退出面板导航或清除起点 | 没有确认弹窗 |
| `1`–`9` | 选择按面板顺序排列的第 1–9 个类别 | 视频已就绪 |
| `0` | 选择按面板顺序排列的第 10 个类别 | 视频已就绪且类别存在 |
| `Ctrl+数字`，再按 `数字` | 先选择类别分组，再选择该组中的类别 | 用于第 11 个及以后类别；组选择状态 3 秒后失效 |
| `M` | 进入参与对象键盘导航 | 已导入检测结果且 track 列表非空 |
| `↑` / `↓` | 在参与对象列表中移动高亮项 | 已进入参与对象导航 |
| `Space` / `Enter` | 选中或取消当前高亮的 track ID | 已进入参与对象导航 |
| `Ctrl+↑` | 选择上一条行为标注 | 标注列表非空 |
| `Ctrl+↓` | 选择下一条行为标注 | 标注列表非空 |
| `E` | 编辑当前选中的行为标注 | 已选中标注且当前允许修改 |
| `Delete` | 删除当前选中的行为标注 | 已选中标注且当前允许修改 |
| `Ctrl+Enter` | 保存标注编辑表单 | 正在编辑标注 |
| `Esc` | 取消标注编辑表单 | 正在编辑标注 |

### 3.3 track 修正模式（计划）

| 按键 | 操作 | 前置条件 |
|---|---|---|
| `↑` | 将 track 列表高亮项上移一项 | track 列表非空，搜索框未聚焦 |
| `↓` | 将 track 列表高亮项下移一项 | track 列表非空，搜索框未聚焦 |
| `Enter` / `Space` | 选中或取消当前高亮 track | 已有高亮 track |
| `S` | 对所选 track 执行 `Split` | 恰好选中 1 条 track |
| `M` | 对所选 track 执行 `Merge` | 至少选中 2 条 track |
| `Delete` | 忽略整个 track | 恰好选中 1 条 track |
| `Ctrl+Z` | 撤销上一次 track 修正 | **计划中的统一撤销**：按实际时间撤销最近一次 Split、Merge 或忽略整个 track |
| `A` | 切换“当前帧”与“全部”track 范围 | — |
| `/` | 聚焦 track 搜索框 | — |
| `Esc` | 先清空搜索；搜索为空时取消全部 track 选择 | 没有确认弹窗 |

### 3.4 确认弹窗和编辑表单

| 场景 | 按键 | 操作 |
|---|---|---|
| 确认弹窗 | `Esc` | 取消并关闭弹窗 |
| 确认弹窗 | `Enter` | 执行确认操作 |
| 标注编辑表单 | `Ctrl+Enter` | 校验并保存 |
| 标注编辑表单 | `Esc` | 放弃修改并退出编辑 |

危险操作仍保留确认弹窗，快捷键不得绕过确认。

## 4. 焦点与优先级规则

一次按键只能触发一个动作，按以下顺序判断：

1. **确认弹窗**：弹窗拥有最高优先级，页面全局快捷键全部暂停。
2. **标注编辑表单**：只处理表单保存、取消和原生文本编辑按键。
3. **搜索框或其他输入控件**：保留文字输入和原生编辑行为；`Esc` 可退出焦点或清空搜索。
4. **参与对象导航状态**：方向键、`Space`、`Enter` 优先操作参与对象列表。
5. **当前工作模式**：执行行为标注或 track 修正的模式专属快捷键。
6. **全局视频与显示控制**：最后处理播放、逐帧、跳转和叠加层显隐。

### 4.1 `Esc` 的统一优先级

`Esc` 按以下顺序只执行第一项可执行操作：

1. 关闭确认弹窗或快捷键帮助；
2. 取消标注编辑；
3. 退出参与对象键盘导航；
4. 清空身份搜索；
5. 取消 track 或标注选中；
6. 清除已设行为起点。

### 4.2 `Space` 的统一优先级

`Space` 按以下顺序处理：

1. 焦点位于输入框时，保留输入行为；
2. 焦点位于按钮时，触发按钮；
3. 正在参与对象或 track 列表键盘导航时，切换当前高亮项；
4. 其他情况下播放/暂停视频。

## 5. 可见反馈

### 5.1 控件内提示

- 现有“设起点”与“设终点”按钮保留 `(S)`、`(D)` 提示。
- track 修正按钮显示快捷键，例如：
  - `Split (S)`；
  - `Merge (M)`；
  - `忽略整个 track (Delete)`；
  - `撤销上一次 track 修正 (Ctrl+Z)`。
- 行为类别按钮显示对应数字角标。
- 当前参与对象或 track 的键盘高亮项，应与“已选中”采用不同视觉状态。

### 5.2 状态提示

以下操作通过页面提示区域反馈：

- 模式切换成功或由于缺少检测导入而失败；
- 起点设置或清除；
- 类别选择；
- 进入/退出参与对象键盘导航；
- 操作前置条件不足，例如“Split 需要恰好选择 1 个 track ID”；
- 撤销成功或当前没有可撤销操作。

### 5.3 快捷键帮助

按 `?` 打开快捷键帮助面板：

- 顶部显示全局快捷键；
- 下方只显示当前工作模式的专属快捷键；
- 支持 `?` 或 `Esc` 关闭；
- 帮助面板打开时暂停其他全局快捷键。

## 6. 冲突检查

| 可能冲突 | 处理规则 |
|---|---|
| `S` 在行为模式用于起点、track 修正模式用于 Split | 两个模式互斥，按 `workspaceMode` 分发 |
| `M` 在行为模式进入对象导航、track 修正模式用于 Merge | 两个模式互斥 |
| `Space` 同时用于播放和列表选中 | 列表导航状态优先于播放 |
| `←` / `→` 与时间轴原生键盘操作冲突 | 时间轴拥有焦点时由时间轴局部处理并阻止冒泡 |
| `Ctrl+Z` 与输入框撤销冲突 | 输入框聚焦时保留浏览器原生撤销 |
| `Ctrl+I` 可能触发浏览器或编辑器斜体 | 标注页非输入状态下 `preventDefault`；输入状态不拦截 |
| `/` 与搜索框文字输入冲突 | 仅搜索框未聚焦时用于聚焦搜索框 |
| 数字键与数字输入框冲突 | 输入框聚焦时不执行类别选择 |
| `Delete` 与文本删除冲突 | 输入框聚焦时保留文本删除 |
| `Enter` 与确认弹窗、表单提交冲突 | 弹窗或表单优先于页面模式快捷键 |

## 7. 边界条件

- 视频未加载完成时，播放、定位和标注快捷键不执行。
- 没有检测结果导入时，不允许切换到 track 修正模式，参与对象快捷键也不执行。
- 类别或 track 索引不存在时，数字键或导航键不执行。
- 类别要求的对象数量不满足时，`D` 不创建标注并显示原因。
- Split、Merge、忽略整个 track 和撤销操作正在请求时，重复按键不再次提交。
- **计划目标**：Split、Merge 和忽略整个 track 进入同一条按时间排序的 track 修正历史；`Ctrl+Z` 使用“撤销上一次 track 修正”，按实际时间只撤销其中最近一项。
- **当前实现边界**：现有前端/后端编排仍有两条撤销路径——track ID 修正与检测抑制分别撤销；统一按时间撤销尚未实现，不能在当前能力说明中声称已经可用。
- 标注列表或 track 列表刷新后，高亮索引应自动修正到有效范围。
- 搜索导致高亮 track 消失时，高亮移动到筛选结果第一项或清空。
- 工作模式切换时退出面板导航状态，避免旧模式状态泄漏。
- 页面刷新后，前端通过 `GET .../detection-suppressions` 恢复当前 active import 中尚未撤销的整轨 suppression 及撤销入口；旧 import 项不列出且撤销返回 409。Split/Merge 的最近操作 ID 仍只保存在当前页面会话中，刷新后应禁用对应撤销快捷键并明确提示。

## 8. 前端实现方案

### 8.1 快捷键调度

继续使用 `AnnotatePage.tsx` 中现有的单一 `window.keydown` 监听器和 `keyHandlerRef` 最新闭包方案，避免多个组件分别注册全局监听器造成重复触发。

建议将判断拆分为以下纯函数或局部处理器：

```text
handleModalShortcut
handleEditFormShortcut
handleFocusedControlShortcut
handleBehaviorShortcut
handleIdentityShortcut
handleGlobalShortcut
```

处理器返回 `boolean` 表示是否已消费该按键。主调度器在第一个返回 `true` 的处理器后停止继续分发。

### 8.2 新增页面状态

建议新增：

```text
activeAnnotationIndex
mouseTrackFocusIndex
identityTrackFocusIndex
categoryGroupPrefix
shortcutHelpOpen
```

避免用 DOM 查询推断业务状态；弹窗状态、编辑状态和列表导航状态应优先由 React state/ref 明确表达。

### 8.3 组件职责

| 文件/组件 | 改动职责 |
|---|---|
| `AnnotatePage.tsx` | 全局快捷键调度、模式状态、动作前置条件和状态提示 |
| `CategoryPanel` | 类别数字角标、当前类别和挂起分组的视觉反馈 |
| `MouseIdsPanel` | 高亮索引、滚动跟随、选中与高亮区分 |
| `AnnotationList` | 当前行选择、高亮、编辑和删除动作暴露 |
| `IdentityPanel` | track 高亮索引、滚动跟随、按钮快捷键提示 |
| `DetectionOverlay.tsx` | 允许父页面控制框、ID、关键点和骨架的显示状态 |
| 快捷键帮助组件 | 展示当前模式快捷键并处理关闭操作 |

## 9. 分阶段实施

### 阶段 1：全局控制

- 10 帧跳转；
- 起点/终点跳转；
- 工作模式切换；
- 检测叠加层显隐；
- 基础冲突调度框架。

### 阶段 2：行为标注全键盘化

- 类别数字选择；
- 参与对象列表导航和选择；
- 标注列表导航；
- 编辑、保存、取消和删除。

### 阶段 3：track 修正全键盘化

- track 列表导航和多选；
- Split、Merge；
- 忽略整个 track；
- 按时间顺序统一“撤销上一次 track 修正”（计划能力，覆盖 Split、Merge 和整轨忽略）；
- 当前帧/全部切换与搜索。

### 阶段 4：可发现性与验收

- 快捷键角标；
- `?` 帮助面板；
- 状态提示；
- 浏览器人工验收和冲突测试。

## 10. 验收标准

1. 用户进入标注页后，不使用鼠标即可完成一条行为标注的完整流程。
2. 不使用鼠标即可选择参与对象、修改和删除已有标注。
3. 不使用鼠标即可完成 Split、Merge 和忽略整个 track，并通过 `Ctrl+Z` 撤销上一次 track 修正（本条是计划验收标准，尚非当前实现）。
4. 所有快捷键在文本输入、确认弹窗和编辑表单中均不会误触页面操作。
5. 同一按键在任意时刻最多触发一个业务动作。
6. 危险操作不会因快捷键绕过确认流程。
7. 快捷键操作的结果、失败原因和当前高亮状态均可见。
8. 鼠标操作继续可用，且与快捷键操作共享同一业务函数，不产生两套行为。
9. Windows 主流浏览器中不覆盖无法阻止的浏览器或系统级快捷键。
10. 前端构建通过，并完成人工键位冲突矩阵验收。

## 11. 待确认事项

- 大步长定位使用固定 10 帧，还是按 1 秒换算帧数；
- 模式切换使用反引号 `` ` ``，还是其他按键；
- 超过 10 个类别时采用“两段数字键”，还是改为类别键盘导航；
- `Ctrl+B`、`Ctrl+I`、`Ctrl+K` 是否接受，或改为不带修饰键的显示控制；
- 行为标注列表导航是否使用 `Ctrl+↑/↓`，还是单独进入列表导航模式；
- Split/Merge 撤销如何从后端历史恢复，以及何时实现跨 Split、Merge、整轨忽略的按时间统一撤销；
- 是否允许方向键长按连续逐帧；
- `Enter` 是否作为危险确认弹窗的确认快捷键。
