# YOLO 检测结果接入与身份校正设计

> 状态：讨论稿，尚未实现
> 日期：2026-08-06
> 分支：`feature/spatial-annotation`
> 核心待定项：身份校正采用“直接操作 YOLO 派生结果”还是“增加 canonical mouse 层”尚未决定。

本文先描述用户能看到和操作的功能，再集中说明数据结构、接口和预计实现方案。所有技术结构均属于候选设计，不能视为已经实现或冻结。

# 第一部分：功能与页面设计

## 1. 功能目标与用户流程

标注网站接收原始视频以及 YOLO Pose/Tracker 产生的完整结构化结果，在原视频上动态显示检测框、ID，并按需显示关键点或骨架连线。标注者可以选定行为涉及的两只鼠，并在 tracker ID 丢失或交换时进行人工修正。

目标流程：

1. 将原始视频、`tracks.jsonl` 和 `metadata.json` 一起拖入同一个上传区域；
2. 页面自动识别三个文件角色，展示配对结果；
3. 文件分别传输并可独立重试，但作为同一个上传批次统一校验和启用；
4. 进入标注工作台，查看与当前帧同步的框和 ID，并按需打开关键点或骨架连线；
5. 标记行为起止区间和行为类别；
6. 依次点击框选中小鼠，并按 `Alt+1`、`Alt+2` 标定为 `mouse_id1`、`mouse_id2`；
7. 如发现 ID 问题，进入身份修正模式，先按帧拆分，再合并正确区间；
8. 返回普通标注模式，保存并提交行为标注。

把框、ID 或骨架直接画进像素的烧录视频不属于上传内容，也不属于正式输入契约。网站使用结构化数据在 Canvas 上动态绘制，因此烧录视频可以完全省去。

## 2. 视频库与一体化导入

默认入口是“上传视频及 YOLO 数据”：用户一次把一组配套文件拖入同一个区域，而不是先后进入两个割裂的上传流程。

### 2.1 同一拖拽区域

上传区域接收三个必需文件：

| 文件角色 | 示例 | 用途 |
|---|---|---|
| 原始视频 | `社交-攻击1.mov` | 播放与标注 |
| 逐帧结果 | `tracks.jsonl` | 框、ID、关键点和置信度 |
| 元数据 | `metadata.json` | 视频、模型、tracker 和骨架定义 |

拖入后页面显示三个文件槽位、文件大小和识别结果。缺少文件、出现多个候选或文件类型无法识别时，用户可以在提交前替换槽位内容。

物理传输允许三个文件分别上传、显示各自进度并独立重试；业务上它们属于同一个上传批次。只有三个文件全部到齐并校验通过后，视频及其 YOLO 功能才统一启用。`tracks.jsonl` 失败时不要求重新上传大视频。

### 2.2 自动配对与校验反馈

系统不能只凭文件名静默配对。应优先读取 `metadata.json` 中的 `source_relative`、`video_id`，并核对视频宽高、FPS、帧数以及 JSONL 的帧覆盖范围。不一致时展示具体差异，让用户替换文件或在允许的容差场景下明确确认。

校验期间显示“上传中、等待其他文件、校验中、可用、校验失败”等批次状态。视频卡片在成功后展示模型版本、帧覆盖范围、检测数量及是否包含 tracker ID。

已有视频仍保留“补传/替换 YOLO 数据”的次要入口。替换前必须展示会受影响的参与鼠引用和身份修正记录，不能静默覆盖。

## 3. 标注工作台布局

在现有标注工作台上增量扩展，不重新设计整套页面。建议结构如下：

```text
┌──────────────────────────────────────┬──────────────────────┐
│ 视频 + Canvas 叠加层                │ 行为类别             │
│                                      ├──────────────────────┤
│ [框 ✓] [ID ✓] [关键点 □] [骨架 □] │ 参与鼠               │
├──────────────────────────────────────┤ mouse_id1 / mouse_id2│
│ 播放、暂停、逐帧、当前帧/时间       ├──────────────────────┤
├──────────────────────────────────────┤ 标注段列表           │
│ 行为时间轴                           │ 身份修正模式：       │
│                                      │ 小鼠列表 + 操作按钮  │
└──────────────────────────────────────┴──────────────────────┘
```

新增区域包括：

- 播放器内 Canvas 叠加层；
- 框、ID、关键点、骨架连线显示开关；
- `mouse_id1`、`mouse_id2` 参与鼠卡片；
- 身份修正模式入口；
- 修正模式下与视频框同步的小鼠列表和拆分/合并按钮。

普通行为标注与身份修正必须是两个明确模式，避免用户在标注行为时误拆分或合并 ID。

## 4. 播放器叠加层

### 4.1 默认展示

- 检测框：独立开关，默认开启；
- tracker ID 或修正后身份：独立开关，默认开启；
- 关键点：独立开关，默认关闭；开启后显示 7 个点位标记；
- 骨架连线：独立开关，默认关闭；使用相同关键点坐标绘制连接关系；
- 选中鼠：使用不同颜色、线宽或标签突出；
- 低置信度关键点及相关骨架段：可以按阈值隐藏，但不从原始数据中删除。

关键点与骨架连线的显示状态相互独立：用户可以只看点位、只看连线、同时显示或全部关闭。关闭任何图层都只影响浏览显示，不改变保存的数据。

### 4.2 播放同步状态

叠加层随视频当前帧更新。页面需要区分：

- 当前帧数据已加载：正常绘制；
- 正在加载：保持视频可操作并显示轻量加载提示；
- 当前帧无检测：显示“本帧无检测结果”，不能误认为系统故障；
- 当前帧不在导入覆盖范围：明确显示数据缺失；
- 查询失败：提供重试，不使用上一帧框冒充当前帧结果。

### 4.3 重叠框选择

多个框重叠时，单击位置可能命中多个候选。页面应显示候选 ID 列表、循环切换候选，或按框面积与中心距离排序，不能始终只选最上层框。

## 5. 双鼠参与者选择

行为标注新增两个参与者槽位，用于展示标定结果：

- `mouse_id1`：第一个参与鼠；
- `mouse_id2`：第二个参与鼠。

点击检测框只改变“当前选中鼠”，不会直接写入参与者槽位。建议交互：

1. 用户建立或选中一个行为时间段；
2. 视频在该时间段内暂停或逐帧定位到清晰画面；
3. 点击第一个框，页面只高亮该框并显示“当前选中鼠”；
4. 按 `Alt+1`，将当前选中鼠标定为 `mouse_id1`；
5. 点击第二个框，再按 `Alt+2` 标定为 `mouse_id2`；
6. 页面同时高亮两个已标定框，并在槽位中显示身份、当前 tracker ID 和选取帧；
7. `Esc` 取消尚未标定的当前选择；重新选框并按 `Alt+1` 或 `Alt+2` 可覆盖对应槽位；
8. 用户仍可通过槽位操作清空或交换两个结果；
9. 保存标注时校验参与者引用是否仍有效。

快捷键仅在标注工作台、已有当前选中框且焦点不位于输入框时生效。选择 `Alt+1`、`Alt+2` 是为了保留普通数字键给后续行为类别快捷键。

`mouse_id1`、`mouse_id2` 当前只是参与者槽位，不自动等同于 actor/target。追逐、接近、回避等方向性行为如何表达主动方与被动方，需要结合行为类别另行决定。

如果所选鼠只在部分行为区间内有可靠身份，页面需要提示身份覆盖范围，而不是默认认为整段均有效。

## 6. 身份修正模式

身份修正用于处理 ID 丢失、新 ID、ID 交换和非小鼠误检框。它与行为标注位于同一个工作台，共用播放器和当前帧，但通过 `[行为标注] [身份修正]` 切换互斥模式。

进入身份修正模式后，行为标注只读，`S`、`D`、`Alt+1`、`Alt+2` 暂停生效。视频仍可播放、暂停和逐帧，选择状态不会因播放而自动清除。

### 6.1 小鼠列表与同步选择

右侧显示“小鼠列表”，顶部提供：

```text
[当前帧 12] [全部 33] [搜索 ID  /]
```

默认使用“当前帧”，只列出当前画面中存在的 ID。每个列表项至少包含：

- 当前显示 ID；
- 第一次出现帧和最后一次出现帧；
- 检测总帧数；
- 当前帧是否可见。

已选 ID 固定显示在列表顶部，即使播放后暂时不在当前帧中也不会消失，而是标记“本帧未出现”。切换到“全部”时显示整段视频中的 ID 摘要，不加载每个 ID 的全部逐帧检测记录。

按 `/` 打开 ID 搜索框，输入数字后按 `Enter` 选中匹配 ID，按 `Esc` 关闭。该快捷键只在身份修正模式且焦点不在其他输入框时拦截，避免影响正常文本输入。

视频检测框上的 ID 与列表 ID 完全一致。用户有两种等价的选择方式：

1. 点击视频中的检测框或框内任意位置；
2. 点击小鼠列表中的对应 ID。

两处选择状态实时同步：在视频中选择后列表项高亮，在列表中选择后视频里的对应框高亮。用户因此可以通过相同 ID 确认选中的是哪只鼠。

身份修正模式采用多选：点击未选中的 ID 会加入选择，点击已选中的 ID 会取消选择。`[清空选择]` 可以一次取消全部。播放视频时选择保持。

默认当前帧列表不会造成明显计算负担，因为它直接使用播放器已经取得的当前帧检测结果，不发起额外查询；前端只在可见 ID 集合变化时更新列表。若“全部”列表数量很大，再使用分页或虚拟滚动。

### 6.2 操作按钮

主要按钮为 `[拆分]`、`[合并]`、`[删除当前框]`、`[删除该 ID 的全部检测]` 和 `[清空选择]`，其启用状态由选中数量和选择来源决定：

| 选中状态 | `[拆分]` | `[合并]` | 删除操作 |
|---|:---:|:---:|---|
| 0 个 ID | 禁用 | 禁用 | 全部禁用 |
| 1 个 ID | 启用 | 禁用 | “删除全部检测”启用；点击了当前帧具体框时“删除当前框”启用 |
| 2 个及以上 ID | 禁用 | 启用 | 全部禁用 |

`[撤销上一步]`、`[操作历史]` 放在次要操作区域，不占用主要流程。`[返回行为标注]` 用于退出身份修正模式。

### 6.3 拆分

选中一个 ID 后，用户可以继续播放或逐帧定位到拆分帧 `F`，再点击 `[拆分]`。

拆分规则固定为：

- 帧号小于 `F` 的记录保持原 ID；
- 从当前帧 `F` 开始及之后的该 ID 记录改为“当前已有最大 ID + 1”；
- 框、关键点和置信度不变。

例如当前最大 ID 为 40，在帧 100 拆分 ID 8：

```text
帧 < 100：仍为 ID 8
帧 ≥ 100：改为 ID 41
```

点击 `[拆分]` 后只弹出一次简洁确认框，显示原 ID、拆分帧、新 ID 和前后范围。用户选择 `[确认拆分]` 或 `[取消]`，不再经过独立的“加入选择—预览—应用”流程。

只有当所选 ID 在 `F` 之前和 `F` 之后都至少有一条记录时才允许拆分。确认后列表新增 ID，默认选中新生成的 ID，便于继续检查或合并。

### 6.4 合并

选中两个及以上 ID 后，`[拆分]` 禁用，`[合并]` 启用。合并作用于这些 ID 在整段视频中的全部记录。

合并后保留“第一次出现时段最早”的 ID：

- 比较每个所选 ID 的第一次出现帧；
- 第一次出现帧最早者作为保留 ID；
- 如果第一次出现帧相同，则保留数值较小的 ID；
- 其他所选 ID 的全部记录统一改为保留 ID。

点击 `[合并]` 后，系统先检查所选 ID 是否在同一帧同时存在。若合并会导致同一个 ID 在同一帧对应多个框，则阻止操作并列出冲突帧；不自动保留高置信度框。

没有冲突时，只显示一次确认框，列出所选 ID、保留 ID 和影响帧数。用户选择 `[确认合并]` 或 `[取消]`。确认后列表只保留目标 ID，并保持其选中状态。

### 6.5 删除误检框

用户在视频中点击一个并非小鼠的具体框后，可以选择：

- `[删除当前框]`：仅删除当前帧的这一条检测；
- `[删除该 ID 的全部检测]`：删除该 ID 在整段视频中的全部检测。

“删除”是用户界面用语。底层不修改或移除原始 YOLO 文件，而是记录为“已忽略检测”，后续叠加渲染、小鼠列表、身份操作和正式导出默认排除这些记录。

删除当前框的确认框显示帧号、ID 和框位置；删除整个 ID 时显示受影响的检测数和帧范围。选择两个及以上 ID 时删除按钮禁用，避免批量误删。

删除操作进入操作历史并支持 `[撤销删除]`。恢复后，检测重新参与渲染、列表统计、身份修正和导出。

### 6.6 常见修正流程

**ID 丢失后出现新 ID：**在列表或视频中选中遮挡前后的两个 ID，点击 `[合并]`。系统自动保留首次出现更早的旧 ID。

**ID 交换：**仍采用先拆分、再合并的两步方法，不增加专用交换按钮。假设帧 `F` 后 ID 10 与 ID 20 互换：

1. 在帧 `F` 单选 ID 10 并拆分，后段得到新 ID；
2. 在同一帧单选 ID 20 并拆分，后段得到另一个新 ID；
3. 多选“ID 10 前段”和属于同一只鼠的另一后段 ID，执行合并；
4. 多选“ID 20 前段”和剩余后段 ID，执行合并；
5. 播放交换点前后视频，检查框上 ID 是否连续。

仅出现一帧的杂质 ID 不自动删除；用户确认它不是小鼠后，可使用“删除当前框”或“删除该 ID 的全部检测”。

### 6.7 历史与审核影响

每次确认拆分、合并、删除或恢复都记录操作者、时间、基础修订号、相关 ID 和影响帧范围。`[撤销上一步]`、`[撤销删除]` 生成恢复结果的新修订，不删除历史。

如果操作影响已被行为标注引用的 ID，确认框同时显示受影响标注数量；涉及已提交或已审核视频时，提示可能退回 `draft`、审核失效并重新生成片段。

列表和按钮使用“ID”这一中性概念。无论底层最终采用方案 A 的派生 YOLO ID，还是方案 B 的 canonical mouse 映射，用户操作保持一致。

## 7. 异常、权限与审核体验

### 7.1 页面异常提示

- 视频与检测元信息不一致：禁止启用叠加层并展示差异；
- 检测只覆盖部分帧：允许查看有效区间，缺失段明确标记；
- 同一身份同帧对应多个框：显示冲突并阻止保存；
- 参与鼠引用被身份修正影响：标记相关行为标注并要求复核；
- 替换检测数据：先展示会受影响的身份编辑和行为标注数量。

### 7.2 权限

建议仅项目 owner、admin、annotator 可选择参与鼠；身份修正是否开放给所有 annotator 或只开放给指定角色待定。审核者默认只读查看修正结果和历史。

### 7.3 审核联动

修改已提交或已审核标注的参与鼠、身份映射或相关框，可能改变导出片段语义。页面应复用现有确认框，说明视频将退回 `draft`、审核结果失效，并清理需要重新生成的片段。

## 8. 产品层面的已确认与待定

已确认：

- 原始视频、`tracks.jsonl` 和 `metadata.json` 在同一拖拽区域中作为一批上传；
- 三个文件可独立传输和重试，但必须统一配对、校验和启用；
- 烧录框、ID、骨架的视频不进入上传内容或正式输入契约；
- 无需生成或上传烧录视频，可以完全省去；
- 框和 ID 默认显示，关键点与骨架连线分别为独立可选项；
- 检测数据按帧区间分段查询，不全量加载；
- 行为标注点击框只做当前选择，再用 `Alt+1`、`Alt+2` 标定 `mouse_id1`、`mouse_id2`；
- 行为标注与身份修正在同一工作台共享播放器，但使用互斥模式和不同操作面板；
- 身份修正通过视频框或小鼠列表同步选择；单选只能拆分，多选只能合并；
- 小鼠列表默认显示当前帧 ID，已选 ID 置顶保留，并提供“全部”和 `/` 快捷搜索；
- 拆分时当前帧之前保留原 ID，当前帧及之后使用最大 ID + 1；
- 合并影响所选 ID 的全部帧，保留首次出现帧最早的 ID，同帧时保留较小 ID；
- 误检框可删除当前检测或该 ID 的全部检测；底层记录为可撤销的忽略操作，不改写原始文件；
- ID 交换使用 Split 后 Merge 的两步流程；
- 身份修正与框/关键点修正分离；
- 原始数据只读，编辑可撤销、可审计、结果可重建。

待定：

- 身份校正采用方案 A 还是方案 B；
- `mouse_id1`、`mouse_id2` 是否进一步表达 actor/target；
- canonical 身份若存在，是视频级还是项目级；
- 身份修正权限由哪些项目角色持有。

本阶段不实现上述功能，也不训练 YOLO 或在网站中执行行为自动识别。

# 第二部分：数据结构、接口与预计实现方案

## 9. 已验证的输入数据契约

2026-08-06 已使用最终 v8 Pose 模型完整处理北医短视频 `社交-攻击1.mov`，样本位于：

```text
行为识别/data/yolo-track-samples/社交-攻击1/
├── metadata.json
└── tracks.jsonl
```

该样本目录中的 `社交-攻击1_all_ids.mp4` 是历史调试产物，不属于上传契约；正式导入只需要原始视频和上述两个结构化文件。

验证事实：

- 视频共 156 帧，`frame_index` 连续覆盖 0～155；
- 共 1877 个检测，全部带 `track_id`；
- 平均每帧约 12 个检测，却产生 33 个不同 tracker ID；
- 11 个 ID 只出现 1 帧，9 个 ID 存在内部中断；
- tracker ID 不能默认视为长期稳定身份。

`tracks.jsonl` 每行对应一帧：

```json
{
  "schema_version": "1.0",
  "video_id": "社交-攻击1",
  "frame_index": 0,
  "timestamp_sec": 0.0,
  "detection_count": 1,
  "detections": [
    {
      "track_id": 1,
      "box_xyxy_px": [100.0, 200.0, 180.0, 310.0],
      "box_xywhn": [0.25, 0.35, 0.04, 0.10],
      "area_n": 0.004,
      "detection_confidence": 0.82,
      "class_id": 0,
      "keypoints": [
        {"x_px": 150.0, "y_px": 210.0, "confidence": 0.99}
      ]
    }
  ]
}
```

`metadata.json` 保存 FPS、宽高、声明与处理帧数、模型路径及 SHA256、tracker、7 个关键点名称、骨架连接边和推理参数。关键点依次为 nose、right ear、left ear、base of neck、left hip、right hip、base of tail。

## 10. 存储分层与共同候选模型

预计分为三层：

```text
不可变上传文件
  ├── 原始视频
  ├── tracks.jsonl
  └── metadata.json
        ↓ 导入解析
可查询检测数据
        ↓ 排除已忽略检测
有效检测数据
        ↓ 身份编辑解析
修正后的身份视图与行为参与者引用
```

共同候选模型：

| 模型 | 用途 |
|---|---|
| `VideoImportBatch` | 聚合原始视频、tracks、metadata 三个文件的上传进度、配对校验和统一启用状态 |
| `DetectionImport` | 记录视频、schema、模型、文件路径、校验状态、修订号和统计 |
| `DetectionFrame` 或 `RawDetection` | 保存按帧可查询的原始框、关键点、tracker ID 和置信度 |
| `DetectionSuppression` | 记录删除当前检测或某 ID 全部检测的忽略范围、操作者和修订 |
| `IdentityEdit` | 记录 Split/Merge、帧区间、操作者、原因和修订 |
| `BoxCorrection` | 独立记录人工框或关键点修正 |
| `AnnotationParticipant` | 保存行为标注的 `mouse_id1`、`mouse_id2` 引用 |

原型预计保留原始 JSONL 文件，同时将每帧检测解析到带 `(video_id, import_revision, frame_index)` 索引的查询存储。物理表采用“一帧一行 JSON”还是“一检测一行”，需在长视频规模测试后决定；前者行数较少，后者查询和约束更细。

## 11. 待定核心决策：两种身份方案

两种方案均为候选，当前不预先确定 canonical mouse 方案。

### 11.1 方案 A：直接操作 YOLO 派生 ID

原始 YOLO 数据保持只读，通过 `IdentityEdit` 生成修正后的派生 track ID。

- Split：把一个原始 ID 的不同帧区间分配给不同派生 ID；
- Merge：把多个原始或派生 ID 区间指向同一派生 ID；
- 行为参与者引用派生 ID；
- 查询时动态应用操作，或生成 `tracks.corrected.jsonl`。

优点是概念直接、单视频实现较轻、下游容易继续消费 YOLO 形态；缺点是多次修订后的 ID 来源、引用迁移和跨视频身份表达较复杂。

### 11.2 方案 B：增加 canonical mouse 层

原始 tracker ID 不变，由 `IdentityAssignment` 将“tracker ID + 帧区间”映射到 `CanonicalMouse`。

- Split：在 assignment 中增加区间边界；
- Merge：让多个区间指向同一 canonical 身份；
- 行为参与者引用 canonical 身份；
- 导出 YOLO 兼容结果时再展开为修正 ID。

优点是原始检测与人工身份边界清晰、适合长时间或跨视频身份；缺点是模型、查询和 UI 状态更复杂，而且必须决定 canonical 身份作用域。

### 11.3 对比与共同底线

无论最终选择哪种底层方案，对用户暴露的 ID 操作语义保持一致：

- Split：帧 `F` 之前保留原显示 ID，帧 `F` 及之后分配当前最大显示 ID + 1；
- Merge：作用于所选显示 ID 的全部帧，保留首次出现帧最早的 ID；首次出现帧相同则保留数值较小的 ID；
- 页面中的小鼠列表、视频框标签和选中状态始终使用同一套显示 ID。

| 维度 | 方案 A：派生 YOLO ID | 方案 B：canonical mouse |
|---|---|---|
| 修正结果 | 操作日志或 corrected JSONL | 帧区间身份映射 |
| 行为标注引用 | 派生 ID | canonical ID |
| 单视频复杂度 | 较低 | 较高 |
| 跨视频身份 | 较弱 | 可支持，作用域待定 |
| 下游 YOLO 兼容 | 较直接 | 需要展开导出 |

共同底线：原始上传数据不可变；每次操作保存操作者、时间、原因和修订号；修改可撤销、可审计；导出结果能够从原始数据和操作记录重建。

## 12. 候选 API

下列路径和字段均未冻结：

| 方法与路径 | 作用 | 关键语义 |
|---|---|---|
| `POST /api/projects/{pid}/video-import-batches` | 创建一体化上传批次 | 返回批次 ID 和 video/tracks/metadata 三个文件槽位 |
| `PUT /api/video-import-batches/{bid}/files/{role}` | 上传批次文件 | `role` 为 `video`、`tracks` 或 `metadata`；分别传输和重试 |
| `POST /api/video-import-batches/{bid}/complete` | 完成上传并校验 | 三个文件到齐后执行配对校验，成功后统一启用 |
| `GET /api/video-import-batches/{bid}` | 查询批次状态 | 返回各文件进度、缺失项、校验错误及已创建视频 |
| `POST /api/videos/{vid}/detection-imports` | 为已有视频补传或替换 YOLO 数据 | 只接收 tracks 与 metadata；替换前返回影响范围 |
| `GET /api/videos/{vid}/detection-imports/current` | 查询当前导入 | 返回状态、错误、模型和帧统计 |
| `GET /api/videos/{vid}/detections` | 分段读取检测 | 参数 `start_frame`、`end_frame`、`identity_view` |
| `GET /api/videos/{vid}/identities` | 获取小鼠列表 | 返回 ID、首次/末次出现帧、检测帧数和当前帧可见状态 |
| `POST /api/videos/{vid}/identity-edits/check` | 校验拆分或合并 | 计算新/保留 ID、冲突帧、影响帧数和受影响标注，供一次确认框使用 |
| `POST /api/videos/{vid}/identity-edits` | 提交身份编辑 | 使用当前修订号，事务校验后创建新修订 |
| `POST /api/videos/{vid}/identity-edits/{id}/revert` | 撤销操作 | 以新修订恢复，不删除审计记录 |
| `POST /api/videos/{vid}/detection-suppressions` | 删除误检 | scope 为单条 detection 或当前 ID 的全部有效检测 |
| `POST /api/videos/{vid}/detection-suppressions/{id}/revert` | 撤销删除 | 生成恢复修订，使相关检测重新可见 |
| `PUT /api/annotations/{aid}/participants` | 保存两个参与鼠 | 保存槽位、身份引用和选取帧 |
| `GET /api/videos/{vid}/detections/export` | 导出修正结果 | 输出 corrected JSONL 或含 canonical 字段的数据 |

分段查询响应至少需要：导入修订号、身份修订号、帧范围、原始 tracker ID、当前显示身份、框、关键点和置信度。前端提交编辑时必须携带它所基于的修订号，避免覆盖他人修改。

## 13. 前端预计实现

预计新增或拆分以下组件：

- `VideoImportDropzone`：同一区域接收三个文件并显示角色槽位、配对结果和批次进度；
- `DetectionOverlayCanvas`：绘制框、ID、关键点和骨架，处理点击命中；
- `OverlayControls`：分别控制框、ID、关键点和骨架连线显示；
- `ParticipantSelector`：展示并管理 `mouse_id1`、`mouse_id2` 标定结果，响应 `Alt+1`、`Alt+2`；
- `MouseIdentityList`：提供当前帧/全部视图、已选置顶、`/` 搜索，并与视频框同步多选；
- `IdentityEditorPanel`：根据选择数量启用拆分、合并或删除，并提供清空、撤销和历史操作。

关键实现点：

1. 使用视频原始宽高与实际可视区域计算缩放和黑边偏移；
2. 用 `currentTime × fps` 映射到 0-based 帧号，并限制在有效范围；
3. 按固定帧块请求当前和相邻区间，使用 LRU 或有限窗口缓存；
4. 跳转后取消无效请求，响应携带修订号，避免旧响应覆盖新数据；
5. canvas 命中测试使用源视频坐标，重叠框返回候选集合；
6. 普通标注模式与身份修正模式使用独立状态机；
7. 关键点标记与骨架连线分别维护显示状态，共用当前帧 keypoints 数据；
8. 参与鼠快捷键复用现有全局键盘守卫，在输入框聚焦或无当前选中框时不触发；
9. 视频框与小鼠列表共享同一组 selected ID 状态，播放时保持选择；
10. 选择一个 ID 时只启用拆分，选择两个及以上时只启用合并；
11. 当前帧列表直接复用已加载检测，只在可见 ID 集合变化时更新；全部列表只请求 ID 摘要，数量很大时分页或虚拟滚动；
12. `/` 在身份修正模式下聚焦 ID 搜索，`Enter` 选择，`Esc` 关闭；
13. 点击拆分或合并后调用 check 接口，成功时直接显示一次确认框；
14. 删除当前框引用具体 detection，删除整个 ID 时显示服务端计算的影响检测数。

框区间大小、预取数量和缓存上限需通过长视频测试确定，不能根据 156 帧样本直接冻结。

## 14. 后端预计实现

预计新增 upload-batch、detection 和 identity 路由与服务层：

1. 创建批次后，为原始视频、tracks 和 metadata 分配独立文件槽位；
2. 三个文件分别流式写入 `/NVme` 对应目录，可独立重试，不把大文件放在系统盘；
3. 文件全部到齐后读取 metadata，通过 `source_relative`、`video_id`、宽高、FPS、帧数和 JSONL 覆盖范围完成配对校验；
4. 批次只有在全部校验通过后才原子化启用；任一结构化文件失败时保留已完成的视频上传；
5. 保留原始视频、`tracks.jsonl` 和 `metadata.json`，逐行解析 JSONL 并写入按帧索引的查询存储；
6. 查询先排除当前修订中已忽略的检测，再解析 Split/Merge 身份结果；
7. 删除当前框直接引用不可变 detection ID；删除整个显示 ID 时，基于当前修订解析并固定受影响 detection 集合，保证后续可审计和恢复；
8. 分段查询读取指定帧的有效检测，再按当前身份修订解析显示身份；
9. check 在事务外计算候选影响，但提交时重新基于当前修订校验；
10. Split 提交时在事务内读取当前有效最大 ID，并将帧 `F` 及之后的所选 ID 改为最大 ID + 1；
11. Merge 提交时比较所选 ID 的首次出现帧，保留最早者；同帧时保留数值较小者，并检查同帧双框和参与者引用；
12. 方案 A 解析为派生 ID，方案 B 解析为 `IdentityAssignment`；
13. 叠加渲染、小鼠列表、冲突检查和导出默认只使用未被忽略的有效检测；
14. 导出任务根据固定修订生成结果，避免编辑过程中导出内容变化。

现有空间标注计划中的 `RawDetection`、`CanonicalMouse`、`IdentityAssignment`、`BoxCorrection`、`AnnotationParticipant` 仍是候选；其中后两者可共用，`CanonicalMouse` 和 `IdentityAssignment` 仅在方案 B 下成立。

## 15. 校验、并发与工作流联动

### 15.1 导入校验

- schema 版本可识别；
- 上传批次恰好包含一个视频、一个 tracks 文件和一个 metadata 文件；
- metadata 中的 `source_relative`、`video_id` 与待关联视频一致；
- `frame_index` 为 0-based、无重复且在视频范围内；
- metadata 与视频宽高、FPS、帧数一致或在明确容差内；
- 框、关键点和置信度字段合法；
- 关键点数量符合模型定义；
- 重复检测只报告，不自动当作同一身份合并。

### 15.2 身份冲突

如果一次 Merge 使同一身份在同一帧对应多个有效框，必须阻止提交或要求人工先删除误检。不能只保留置信度最高框而不留下人工决定记录。

### 15.3 并发与修订

检测导入和身份编辑均使用递增修订号。提交时检查基础修订；不一致则要求刷新列表并重新确认。身份编辑、参与者引用更新、审核失效应在同一事务边界或可恢复工作流内处理。

### 15.4 审核与片段失效

修改参与鼠、身份映射或相关框可能改变已审核事件语义。预计沿用现有机制：视频退回 `draft`，旧审核失效，相关 Clip 与缩略图清理后在重新审核通过时生成。

## 16. 分阶段实施建议

以下只是后续顺序，本阶段不实施：

1. 固化三文件上传批次、帧编号、坐标和查询响应契约；
2. 实现一体化拖拽、独立重试、配对校验、状态查询和按帧区间读取；
3. 实现只读框、ID，以及可独立开关的关键点和骨架连线叠加；
4. 实现 `mouse_id1`、`mouse_id2` 双鼠选择；
5. 决定方案 A/B 后实现混合小鼠列表、搜索、单选拆分、多选合并、误检删除、冲突检查和撤销；
6. 实现独立的 BoxCorrection；
7. 完成审核联动、导出和长视频性能验收。

## 17. 待讨论问题

1. 身份修正最终采用方案 A 还是方案 B？
2. 方案 A 是否必须生成实体 `tracks.corrected.jsonl`？
3. canonical 身份若存在，是视频级还是项目级？
4. 一帧内重复检测如何选择、屏蔽或删除？
5. `mouse_id1`、`mouse_id2` 是否需要表达 actor/target？
6. 身份修正权限开放给哪些项目角色？
7. 关键点和骨架连线分别使用什么置信度阈值，是否允许用户调整？
8. 分段查询区间、预取范围和缓存上限是多少？
9. 浏览器使用转码视频时，如何保证其帧序号与原始检测严格对齐？
10. 替换 YOLO 导入结果时，已有身份编辑和参与者引用如何迁移？
